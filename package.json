{
  "name": "greenlogix-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EcoMiles — B2B Urban Logistics Optimization & ISO 14083 Carbon Accounting Platform",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "dev:landing": "pnpm --filter @greenlogix/landing dev",
    "dev:api": "cd apps/api && uv run uvicorn greenlogix_api.main:app --port 8000 --reload",
    "build:landing": "pnpm --filter @greenlogix/landing build",
    "preview:landing": "pnpm --filter @greenlogix/landing preview",
    "test:api": "cd apps/api && uv run pytest",
    "test:worker": "pnpm --filter @greenlogix/worker test",
    "test": "pnpm test:worker && pnpm test:api",
    "deploy:landing": "pnpm --filter @greenlogix/landing exec wrangler pages deploy dist --project-name=greenlogix --branch=main --commit-dirty=true",
    "deploy:ecomiles": "pnpm --filter @greenlogix/landing exec wrangler pages deploy dist --project-name=ecomiles --branch=main --commit-dirty=true",
    "build": "pnpm --filter \"./apps/*\" build"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.126.0"
  }
}
