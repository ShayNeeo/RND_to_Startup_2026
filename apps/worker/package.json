{
  "name": "@greenlogix/worker",
  "version": "1.0.0",
  "description": "EcoMiles Cloudflare Edge Worker API Gateway and Proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.127.1"
  }
}
