---
schema: change-request/v1
cr_id: CR-20260913-001
title: "RoadBaseline OSM adapter + optional eco-cost (no Google scrape)"
status: in_progress
priority: high
risk: medium
base_branch: so2026
created_at: 2026-09-13
requester: "Phạm Quốc Thanh (@ShayNeeo) / Cursor Cloud Agent"
integrator: "Phạm Quốc Thanh (@ShayNeeo)"
---

# Change Request: `CR-20260913-001`

> Integrator (human or AI): read **§1 then §3** before looking at the diff.

## 1. Outcome

**Objective** (one sentence): Replace solver tour km with a `RoadBaseline` interface backed by free OSM (Valhalla auto/truck or OSRM driving) plus circuity fallback, and add an optional eco-weighted routing cost hook, so a Google Directions key can plug in later without scraping.

**Why now:** SO 2026 contest novelty must be shipped in code before 2026-09-25. No Google Maps key exists. Baseline vs optimized remains Excel-order vs NN+2-opt (not Google).

**Base:** `origin/so2026` @ `26857792eaf44866434bded476f018ead8176d7a`

## 2. Scope fence (fill this first)

### MUST KEEP

- Demo auth: dispatcher `Bearer DEMO` / `?token=DEMO`, driver PIN `0000`.
- Vehicle type `xe_tai_nho` (vans/small trucks), capacity 500–2000 kg. Not motorcycles.
- Cluster radius stays geographic haversine (default 3 km).
- Late-risk ETA stays circuity `road_km` at 30 km/h (no live traffic).
- Baseline vs optimized = spreadsheet-order zig-zag vs clustered NN+2-opt.
- CO₂ remains TTW: `kg_co2 = km × (L/100km) × kg_co2_per_litre`. Not a full ISO 14083 pack.
- Frozen OpenAPI path set and `OptimizeIn.cluster_radius_km` contract.
- Existing landing Role Portal + worker `/app` `/driver` demo flow.

### MUST CHANGE

- [x] **C-01:** `RoadBaseline` protocol + circuity fallback + OSM backends (Valhalla and/or OSRM) + Google stub (not configured / not scraped).
- [x] **C-02:** Wire matrix/pair lookups into API and worker solver tour + NN+2-opt. HTTP timeout then fallback. CI must not require Docker OSM extracts.
- [x] **C-03:** Optional eco-cost hook (`GREENLOGIX_ECO_WEIGHT` in `[0,1]`) used as sequencing cost; reported km/CO₂ stay physical TTW.
- [x] **C-04:** Tests: baseline ≠ optimized still; adapter + eco-cost hook.
- [x] **C-05:** Docs/UI copy: Google-class baseline = OSM road network for now; Google key later. Update `implementation-notes.md`.

### MUST NOT TOUCH

- `apps/landing/**` marketing rewrite (Role Portal / pitch deck stay).
- `apps/mobile-driver/**`
- `apps/api/openapi.json` / frozen OpenAPI contract
- `apps/api/data/emission_factors.json` factor values
- Google scraping, paid map keys, ISO 14083 inventory rewrite

### Explicit exceptions

| Path | Allowed delta | Why |
|------|----------------|-----|
| `apps/api/templates/dispatcher.html` | Distance/CO₂ note only | Jury copy |
| `apps/worker/src/dispatcherHtml.ts` | Distance/CO₂ note only | Live demo copy |
| `README.md` / `apps/api/README.md` | Distance paragraph | Contest docs |

## 3. Ownership

| Worktree | Owner | Type | Branch | Paths | Status |
|----------|-------|------|--------|-------|--------|
| WT-01 | Thanh / cloud agent | ai | `cursor/road-baseline-eco-6536` | `apps/api/src/greenlogix_api/solver/**`, `apps/api/tests/test_road_baseline.py`, `apps/api/tests/test_eco_cost.py`, `apps/worker/src/roadBaseline.ts`, `apps/worker/src/solver.ts`, `apps/worker/src/index.ts`, docs | In progress |

| Task | Description | Owner | WT | Depends | Status |
|------|-------------|-------|----|---------|--------|
| T-01 | RoadBaseline + OSM + fallback + tests | agent | WT-01 | — | In progress |
| T-02 | Eco-cost hook + solver wiring | agent | WT-01 | T-01 | Not Started |
| T-03 | Worker mirror + UI/docs | agent | WT-01 | T-01 | Not Started |

**Hotspots this CR (one owner):**

| Path | Owner |
|------|-------|
| `apps/api/openapi.json` | do not edit |
| lockfiles | do not edit |

## 4. Acceptance

- [x] **AC-01:** `RoadBaseline` abstraction + at least one OSM implementation + circuity fallback when HTTP fails.
- [x] **AC-02:** Existing tests/build for touched apps still pass. Baseline km ≠ optimized km.
- [x] **AC-03:** No files outside MUST CHANGE / exceptions.
- [x] **AC-04:** `implementation-notes.md` has What changed / Decisions / Verification.

### Verify commands

```bash
export PATH="$HOME/.local/bin:$PATH"
cd apps/api && uv sync --locked && uv run --locked pytest -q
pnpm --filter @greenlogix/worker run typecheck
node --experimental-strip-types --test apps/worker/src/roadBaseline.test.ts
```

## 5. Integrator checklist (merge to `so2026`)

- [ ] Diff `origin/so2026...HEAD` contains no **MUST NOT TOUCH** paths
- [ ] Every **MUST CHANGE** item is done or explicitly cut
- [ ] **MUST KEEP** still holds
- [ ] Verify commands ran (paste output summary)
- [ ] PR targets `so2026` (contest branch) and links this file
- [ ] Decision: **approve** / **request changes** (cite CR lines)

Integrator notes:

## 6. Agent completion (each WT)

```yaml
cr_id: CR-20260913-001
task_ids: [T-01, T-02, T-03]
worktree_id: WT-01
branch: cursor/road-baseline-eco-6536
base_sha: "26857792eaf44866434bded476f018ead8176d7a"
final_sha: ""
files_changed: []
tests: [{ command: "", result: "" }]
unresolved_items: []
```

## 7. After merge

- [ ] CR moved to `changes/archive/`
- [ ] Worktrees removed
- [ ] PR URL:
