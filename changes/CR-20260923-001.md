---
schema: change-request/v1
cr_id: CR-20260923-001
title: "Frontier loop-until-done: close 09-23 remaining gaps smallest-first"
status: review
priority: high
risk: medium
base_branch: dev
created_at: 2026-09-23
requester: "Thanh / @ShayNeeo"
integrator: "Thanh / @ShayNeeo"
---

# Change Request: `CR-20260923-001`

> Integrator (human or AI): read **§1 then §3** before looking at the diff.

## 1. Outcome

**Objective** (one sentence): Close FRONTIER_GAP_AUDIT_2026-09-23.md §2 remaining gaps smallest-first until all 16 CRs meet strict PASS or record honest BLOCKED with evidence.

**Why now:** Audit 09-23 verdict 0/16/0 strict PASS (16 PARTIAL, 407 pytest green); demo honesty slice requires lockout, migration, feedback closures before tiles/PostGIS/JWT infra.

**Base:** `origin/dev` @ `dev-head` (work continues on `so2026` loop branch; merge to `dev` via PR after gate)

## 2. Scope fence (fill this first)

### MUST KEEP

- Frozen OpenAPI path set additive-only (18 paths incl. GET /routes/{id}/admin-areas); no path removal/rename without contract update
- `routing_quality` VERIFIED_GRAPH | DEGRADED fail-closed posture (`publish_blocked_for_quality` single decision point)
- Manifest 7-key propagation + C1-illustrative not-certified wording contract (`carbon.py:18-28`)
- Demo auth fail-closed (`GREENLOGIX_DEMO=1` gate); no global PIN in prod config
- No invented GOFA/Valhalla/DOI/truck-safe claims (BLOCKED notes over guessing)
- Deterministic seed mode + 407-test green baseline (no regression)

### MUST CHANGE

- [x] **C-01:** Lockout enforcement — turn `_pin_lockout_active`/`_record_pin_attempt` stub into enforced threshold (Redis-independent in-memory, threshold response + tests)
- [x] **C-02:** Vehicle DB envelope completion — verify/fill missing envelope fields migration path (height/width/length/GVW/axle/powertrain/rated-L/aero) consumed by per-vehicle wiring
- [x] **C-03:** Restriction feedback endpoint — `POST /v1/driver/restriction-feedback` (or root-prefix equivalent) + verification queue (no auto-mutate prod routing) + tests
- [x] **C-04:** CORS prod ops value — per-deploy `GREENLOGIX_CORS_ORIGINS` verified, no `*` outside demo (docs + `.env.example` + test)
- [x] **C-05:** Pareto/ALNS router wiring remainder — expose verified-path + adaptive trace where tests prove gap (no optimality invention)
- [x] **C-06:** Audit refresh — new dated `FRONTIER_GAP_AUDIT_2026-09-24.md` (priors untouched) + `implementation-notes.md` append

### MUST NOT TOUCH

- `apps/landing/**` (marketing site; PWA copy tweak only if honesty label requires, else out)
- `apps/mobile-driver/**` (Flutter; maps_link already fixed, no new nav claims)
- `apps/worker/**` (edge duplicate; FastAPI authoritative per ADR 0001)
- `docs/planning/**` (finance numbers owned by Phuc+Khang)
- `docs/contests/**` (contest packet owned by Khang)
- `pnpm-lock.yaml` `pnpm-workspace.yaml` lockfiles + `apps/api/data/emission_factors.json`
- `GREENLOGIX_FRONTIER_PLAN_2026-09-1.md` (ground truth, read-only)
- `FRONTIER_GAP_AUDIT_2026-09-21.md` + `FRONTIER_GAP_AUDIT_2026-09-22.md` + `FRONTIER_GAP_AUDIT_2026-09-23.md` (priors untouched)
- Live secrets / API keys (never commit)

### Explicit exceptions

| Path | Allowed delta | Why |
|------|----------------|-----|
| `apps/api/openapi.json` | Additive only + regen via `dump_openapi` | New feedback endpoint adds one path; contract test updated |
| `apps/api/tests/test_contract.py` | `FROZEN_METHODS` +1 entry | Allow new feedback path explicitly |
| `apps/landing/public/driver/index.html` | Honesty-label line only | Only if audit demands disclaimer fix |

## 3. Ownership

| Worktree | Owner | Type | Branch | Paths | Status |
|----------|-------|------|--------|-------|--------|
| WT-01 | worker-lockout | ai | `so2026` (loop) | `apps/api/src/greenlogix_api/auth/*`, `tests/test_auth*.py` | Planned |
| WT-02 | worker-vehicle | ai | `so2026` (loop) | `apps/api/src/greenlogix_api/models.py`, `geo/truck_profile.py`, `solver/*`, `tests/test_truck*.py` | Planned |
| WT-03 | worker-feedback | ai | `so2026` (loop) | `apps/api/src/greenlogix_api/geo/restrictions.py`, `routers/*feedback*`, `tests/test_*feedback*.py` | Planned |

| Task | Description | Owner | WT | Depends | Status |
|------|-------------|-------|----|---------|--------|
| T-01 | Lockout enforcement + tests | worker-lockout | WT-01 | — | Done (133 passed) |
| T-02 | Vehicle envelope migration check + tests | worker-vehicle | WT-02 | — | Done (146 targeted, 418 full) |
| T-03 | Restriction feedback endpoint + queue + tests | worker-feedback | WT-03 | — | Done (102 targeted) |
| T-04 | CORS ops verify + audit refresh + notes | orchestrator | — | T-01..T-03 | Done (C-04 verified, 09-24 audit + notes) |

**Hotspots this CR (one owner):**

| Path | Owner |
|------|-------|
| `apps/api/openapi.json` | worker-feedback (only if new path added; else untouched) |
| lockfiles | none (untouched) |

## 4. Acceptance

- [x] **AC-01:** Lockout enforced with threshold test (no demo regression)
- [x] **AC-02:** Existing tests/build for touched apps still pass (407+ green, zero regression)
- [x] **AC-03:** No files outside MUST CHANGE / exceptions.

### Verify commands

```bash
cd apps/api && uv run pytest -q
# contract: test_frozen_openapi green (or updated +1 with regen note)
# banned claims: grep -rni "truck-safe\|ISO.*certified" apps/api/src --include="*.py" | verify negations only
```

## 5. Integrator checklist (merge to `dev`)

- [ ] Diff `origin/dev...HEAD` contains no **MUST NOT TOUCH** paths
- [ ] Every **MUST CHANGE** item is done or explicitly cut
- [ ] **MUST KEEP** still holds
- [ ] Verify commands ran (paste output summary)
- [ ] PR uses `feature-to-dev` template and links this file
- [ ] Decision: **approve** / **request changes** (cite CR lines)

Integrator notes: Reviewer PASS 425 green. T-01 lockout enforced (429 pin_locked_out, failed-only counting). T-02 Vehicle +3 nullable (axle_load_t, frontal_area_m2, cd) + ALTER helper + serialize passthrough + openapi regen additive. T-03 POST /driver/restriction-feedback 201 + queue (never mutates ACTIVE_RULES) + FROZEN +1 (19 paths). C-05 optimizer/pareto_source audit keys (extra only). C-06 audit 09-24 + notes append. Banned-claims negations only. Merge-gate note: cumulative dev...HEAD contains pre-existing landing/worker diffs outside T-scope; rule on those at PR.

## 6. Agent completion (each WT)

```yaml
cr_id: CR-20260923-001
task_ids: [T-01]
worktree_id: WT-01
branch: so2026
base_sha: ""
final_sha: ""
files_changed: []
tests: [{ command: "", result: "" }]
unresolved_items: []
```

## 7. After merge

- [ ] CR moved to `changes/archive/`
- [ ] Worktrees removed
- [ ] PR URL:
