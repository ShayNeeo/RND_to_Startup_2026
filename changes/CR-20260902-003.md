---
schema: change-request/v1
cr_id: CR-20260902-003
title: Phase 2 Chiến — order filter, late-risk, report.xlsx
status: in_progress
priority: high
risk: low
base_branch: feature/landing-qa-72h
created_at: 2026-09-02
requester: Chiến / gsd-executor 02-00
integrator: Thanh
---

# Change Request: `CR-20260902-003`

> Integrator (human or AI): read **§1 then §3** before looking at the diff.

## 1. Outcome

**Objective** (one sentence): Dispatcher can search orders, flag haversine 30 km/h late-risk vs `window_end`, and download the same before/after report as xlsx — zero paid map APIs.

**Why now:** Phase 2 bắt buộc leftovers after the 72h walking skeleton (filter + late-risk + report.xlsx).

**Base:** `feature/landing-qa-72h` @ `ac11eb1dbf096d79b33dc2501a7105870d1fdf3d`

## 2. Scope fence (fill this first)

### MUST KEEP

- Seed 80/10, Optimize, Publish, PATCH/DELETE /orders/{id} (409 after publish)
- Haversine × `HCMC_CIRCUITY=1.35`; no Nominatim/Goong/Mapbox/traffic keys
- Dispatcher map + KPI/TTW strip above the fold; OSM tiles only
- `GREENLOGIX_DEMO=1` Bearer DEMO / PIN 0000
- GET /report JSON numbers (xlsx must match, not replace)

### MUST CHANGE

- [ ] **C-01:** `OrderOut.late_risk`; GET /orders?q=&late=1 (address/phone/receiver subset; late = ETA vs window_end)
- [ ] **C-02:** GET /report.xlsx openpyxl workbook, same totals as GET /report
- [ ] **C-03:** Dispatcher `input#q`, `checkbox#late`, Tải báo cáo xlsx; map stays above the fold

### MUST NOT TOUCH

- `apps/landing/**`
- `apps/mobile-driver/**` (02-01)
- `apps/web-portal/**`
- Paid map/routing/traffic integrations
- `docs/contest/**` numbers
- `.env` keys / sudo

### Explicit exceptions

| Path | Allowed delta | Why |
|------|----------------|-----|
| `apps/api/openapi.json` | Additive `late_risk`, query params, `/report.xlsx` | Flutter 02-01 reads the field |

## 3. Ownership

| Worktree | Owner | Type | Branch | Paths | Status |
|----------|-------|------|--------|-------|--------|
| WT-02-00 | Chiến | ai | `feature/landing-qa-72h` | `apps/api/**` except Flutter/landing | Active |

| Task | Description | Owner | WT | Depends | Status |
|------|-------------|-------|----|---------|--------|
| T-01 | late.py + GET /orders filters + tests | Chiến | WT-02-00 | — | In Progress |
| T-02 | GET /report.xlsx | Chiến | WT-02-00 | T-01 | Not Started |
| T-03 | Dispatcher search / late / xlsx | Chiến | WT-02-00 | T-02 | Not Started |

**Hotspots this CR (one owner):**

| Path | Owner |
|------|-------|
| `apps/api/openapi.json` | Chiến |
| lockfiles | nobody (do not touch pnpm-lock) |

## 4. Acceptance

- [ ] **AC-01:** GET /orders?q=Q1 is a subset; empty q returns all; ?late=1 is only `late_risk` true
- [ ] **AC-02:** GET /report.xlsx spreadsheetml; cells match GET /report
- [ ] **AC-03:** Dispatcher has #q, #late, Tải báo cáo xlsx; Seed/Optimize/Publish/PATCH/DELETE still in HTML
- [ ] **AC-04:** `cd apps/api && uv run pytest` including `tests/test_late.py` and `tests/test_report_xlsx.py`

### Verify commands

```bash
cd apps/api && uv run pytest -x tests/test_late.py tests/test_report_xlsx.py tests/test_orders_crud.py tests/test_report.py tests/test_tracer_e2e.py
```

## 5. Integrator checklist (merge to `dev`)

- [ ] Diff contains no **MUST NOT TOUCH** paths
- [ ] Every **MUST CHANGE** item is done or explicitly cut
- [ ] **MUST KEEP** still holds
- [ ] Verify commands ran
- [ ] PR uses `feature-to-dev` template and links this file
- [ ] Decision: **approve** / **request changes** (cite CR lines)

Integrator notes:

## 6. Agent completion (each WT)

```yaml
cr_id: CR-20260902-003
task_ids: [T-01]
worktree_id: WT-02-00
branch: feature/landing-qa-72h
base_sha: "ac11eb1dbf096d79b33dc2501a7105870d1fdf3d"
final_sha: ""
files_changed: []
tests: [{ command: "cd apps/api && uv run pytest", result: "pending" }]
unresolved_items: []
```

## 7. After merge

- [ ] CR moved to `changes/archive/`
- [ ] Worktrees removed
- [ ] PR URL:
