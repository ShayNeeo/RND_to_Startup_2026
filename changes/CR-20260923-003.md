---
schema: change-request/v1
cr_id: CR-20260923-003
title: "ALNS ban-window conflict destroy op + tests (code-closable slice)"
status: review   # draft | ready | in_progress | blocked | review | approved | merged | cancelled
priority: medium
risk: low
base_branch: dev
created_at: 2026-09-23
requester: "Thanh / @ShayNeeo"
integrator: "Thanh / @ShayNeeo"
---

# Change Request: `CR-20260923-003`

> Integrator (human or AI): read **§1 then §3** before looking at the diff.

## 1. Outcome

**Objective** (one sentence): Close FRONTIER_GAP_AUDIT_2026-09-27.md §2 item 4 sub-slice (ban-window conflict destroy op) at code level with deterministic tests, no contract churn.

**Why now:** 09-27 verdict 0/16/0 strict PASS (16 PARTIAL, 439 pytest green); §2 item 4 ALNS remainder lists ban-window destroy op first among code-closable slices (payload-bin cache, restarts, ablation remain after); §2 items 1–2 are ops-side / BLOCKED on sponsor docs.

**Base:** `origin/dev` @ `e47dbff` (code continues on `so2026` loop branch @ `370900c`, which holds the unmerged ALNS v2 code this CR depends on; see §3 branch note)

## 2. Scope fence (fill this first)

### MUST KEEP

- Frozen OpenAPI path set additive-only (21 paths); no path removal/rename without contract update
- `routing_quality` VERIFIED_GRAPH | DEGRADED fail-closed posture (`publish_blocked_for_quality` single decision point)
- Manifest 7-key propagation + C1-illustrative not-certified wording contract (`carbon.py:18-28`)
- Demo auth fail-closed (`GREENLOGIX_DEMO=1` gate); no global PIN in prod config
- Feedback NEVER auto-mutates `ACTIVE_RULES` (no promotion-to-rules path)
- Deterministic seed mode + 439-test green baseline (no regression)
- No invented GOFA/Valhalla/DOI/truck-safe claims (BLOCKED notes over guessing)

### MUST CHANGE

- [x] **C-01:** Ban-window conflict destroy op — `ban_window_removal_destroy` in `optimizer/eco_alns.py` (Decision 23/2018 overlap via `geo/restrictions.check_truck_ban`, per-tour `profile.vehicle_class`, binary restricted-first scoring, seeded-rng exact-tie shuffle only), registered in `DESTROY_OPS` + adaptive `destroy_weights`, docstring operator list updated (library-level only, hill-climb default unchanged)
- [x] **C-02:** Tests + notes — deterministic same-seed, restricted-first ordering, feasible via solver (`destroy_op="ban_window"`), registration/adaptive-selectable; `implementation-notes.md` append + new dated gap audit (priors untouched)

### MUST NOT TOUCH

- `apps/landing/**` (marketing site)
- `apps/mobile-driver/**` (Flutter; maps_link already fixed)
- `apps/worker/**` (edge duplicate; FastAPI authoritative per ADR 0001)
- `docs/planning/**` (finance numbers owned by Phuc+Khang)
- `docs/contests/**` (contest packet owned by Khang)
- `pnpm-lock.yaml` `pnpm-workspace.yaml` lockfiles + `apps/api/data/emission_factors.json`
- `GREENLOGIX_FRONTIER_PLAN_2026-09-1.md` (ground truth, read-only)
- `FRONTIER_GAP_AUDIT_2026-09-21.md` + `2026-09-22.md` + `2026-09-23.md` + `2026-09-24.md` + `2026-09-25.md` + `2026-09-26.md` + `2026-09-27.md` (priors untouched)
- `apps/api/openapi.json` + `apps/api/tests/test_contract.py` (no contract change this slice)
- Live secrets / API keys (never commit)

### Explicit exceptions

| Path | Allowed delta | Why |
|------|----------------|-----|
| `implementation-notes.md` | Append only | Running log per craftsmanship invariant |
| `FRONTIER_GAP_AUDIT_2026-09-28.md` | New file only | Dated revision, priors untouched |

## 3. Ownership

| Worktree | Owner | Type | Branch | Paths | Status |
|----------|-------|------|--------|-------|--------|
| WT-01 | loop-agent | ai | `so2026` (loop; see note) | `apps/api/src/greenlogix_api/optimizer/eco_alns.py`, `apps/api/tests/test_alns_operators.py`, `implementation-notes.md`, `FRONTIER_GAP_AUDIT_2026-09-28.md` | In Progress |

Branch note (needs integrator ack): rules say cut `cr/<nnn>-<scope>` from `origin/dev`, but ALNS v2 code (`optimizer/eco_alns.py` SA/budget/trace + worst_fuel/uphill ops) lives only on `so2026` (370900c, 21 commits ahead). Recommended: single-agent loop on `so2026`, additive-only, no parallel workers — same precedent as 09-25/09-26/09-27 closures.

| Task | Description | Owner | WT | Depends | Status |
|------|-------------|-------|----|---------|--------|
| T-01 | Ban-window destroy op (lib + registration + weights) | loop-agent | WT-01 | — | In Progress |
| T-02 | Tests + notes append + dated audit | loop-agent | WT-01 | T-01 | Not Started |

**Hotspots this CR (one owner):**

| Path | Owner |
|------|-------|
| `apps/api/openapi.json` | none (untouched) |
| lockfiles | none (untouched) |

## 4. Acceptance

- [x] **AC-01:** Same seed gives identical removal order; restricted-window stops removed before unrestricted ones; solver with `destroy_op="ban_window"` stays feasible (capacity respected, unassigned bounded)
- [x] **AC-02:** Existing tests/build for touched apps still pass (439+ green, zero regression; contract 21 paths unchanged)
- [x] **AC-03:** No files outside MUST CHANGE / exceptions; `ACTIVE_RULES` byte-identical (read-only check, never mutated)

### Verify commands

```bash
cd apps/api && uv run pytest tests/test_alns_operators.py tests/test_eco_alns.py -q
cd apps/api && uv run pytest -q
# contract: test_frozen_openapi green (21 paths, unchanged)
# banned claims: grep -rni "truck-safe\|ISO.*certified" src --include="*.py" | verify negations only
```

## 5. Integrator checklist (merge to `dev`)

- [ ] Diff `origin/dev...HEAD` contains no **MUST NOT TOUCH** paths
- [ ] Every **MUST CHANGE** item is done or explicitly cut
- [ ] **MUST KEEP** still holds
- [ ] Verify commands ran (paste output summary)
- [ ] PR uses `feature-to-dev` template and links this file
- [ ] Decision: **approve** / **request changes** (cite CR lines)

Integrator notes: Reviewer PASS (BanWindowReviewer, AC-01/02/03 all PASS). Targeted `test_alns_operators.py + test_eco_alns.py` 14 passed; full suite 442 passed (was 439, +3). Contract 21 paths unchanged, `test_frozen_openapi` green. Banned-claims negations only. Loop runs on so2026 (ALNS v2 code lives here); merge-gate note: cumulative dev...HEAD contains pre-existing landing/worker diffs outside slice scope — rule on those at PR.

## 6. Agent completion (each WT)

```yaml
cr_id: CR-20260923-003
task_ids: [T-01, T-02]
worktree_id: WT-01
branch: so2026
final_sha: "uncommitted-so2026-ban-window-slice"
files_changed: ["apps/api/src/greenlogix_api/optimizer/eco_alns.py", "apps/api/tests/test_alns_operators.py", "changes/CR-20260923-003.md", "implementation-notes.md", "FRONTIER_GAP_AUDIT_2026-09-28.md"]
tests: [{ command: "cd apps/api && uv run pytest tests/test_alns_operators.py tests/test_eco_alns.py -q", result: "14 passed" }, { command: "cd apps/api && uv run pytest -q", result: "442 passed" }, { command: "cd apps/api && uv run pytest tests/test_contract.py -q", result: "87 passed, 21 paths unchanged" }]
unresolved_items: ["payload-bin cache", "multiple restarts", "full v1/v2 ablation rows", "route audit drawer endpoint"]
```

## 7. After merge

- [ ] CR moved to `changes/archive/`
- [ ] Worktrees removed
- [ ] PR URL:
