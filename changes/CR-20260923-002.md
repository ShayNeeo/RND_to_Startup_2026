---
schema: change-request/v1
cr_id: CR-20260923-002
title: "Feedback admin review endpoints: list pending + verify (no auto-promotion)"
status: blocked   # draft | ready | in_progress | blocked | review | approved | merged | cancelled
priority: medium
risk: low
base_branch: dev
created_at: 2026-09-23
requester: "agent (loop)"
integrator: "Thanh / @ShayNeeo"
---

# Change Request: `CR-20260923-002`

> Integrator (human or AI): read **§1 then §3** before looking at the diff.

## 1. Outcome

**Objective** (one sentence): Close FRONTIER_GAP_AUDIT_2026-09-24.md §2 item 3 (feedback admin promotion workflow missing) at code level with manager-only review endpoints over the existing in-memory queue, without any auto-mutation of production routing.

**Why now:** CR-20260923-001 (status: review, 425 pytest green, 19 paths) delivered driver submit + library-level `verify_feedback`; the queue is unreachable over HTTP for admin review, which is the smallest remaining code-closable gap (§2 items 1–2 are ops-side / BLOCKED on sponsor docs).

**Base:** `origin/dev` @ `e47dbff` (code continues on `so2026` loop branch @ `370900c`, which holds the unmerged CR-001 queue code this CR depends on; see §3 branch note)

## 2. Scope fence (fill this first)

### MUST KEEP

- Frozen OpenAPI path set additive-only (19 paths incl. `POST /driver/restriction-feedback`); no path removal/rename without contract update
- `routing_quality` VERIFIED_GRAPH | DEGRADED fail-closed posture (`publish_blocked_for_quality` single decision point)
- Manifest 7-key propagation + C1-illustrative not-certified wording contract (`carbon.py:18-28`)
- Demo auth fail-closed (`GREENLOGIX_DEMO=1` gate); no global PIN in prod config
- Feedback NEVER auto-mutates `ACTIVE_RULES` (`geo/restrictions.py:verify_feedback` never touches `ACTIVE_RULES`; new endpoints call it only — no promotion-to-rules path)
- Deterministic seed mode + 425-test green baseline (no regression)

### MUST CHANGE

- [ ] **C-01:** Admin review endpoints — `GET` pending-feedback list + `POST` verify-feedback `{approved}` over existing `_FEEDBACK_QUEUE`, manager-role only (`require_manager_role`), 404 on unknown id (no new dependencies, no DB migration, stdlib only)
- [ ] **C-02:** Contract regen additive-only via `dump_openapi` + `FROZEN_METHODS` entries + targeted tests (submit→list→verify→queue-empty, cross-role 403, unknown-id 404, `ACTIVE_RULES` untouched)

### MUST NOT TOUCH

- `apps/landing/**` (marketing site)
- `apps/mobile-driver/**` (Flutter; maps_link already fixed)
- `apps/worker/**` (edge duplicate; FastAPI authoritative per ADR 0001)
- `docs/planning/**` (finance numbers owned by Phuc+Khang)
- `docs/contests/**` (contest packet owned by Khang)
- `pnpm-lock.yaml` `pnpm-workspace.yaml` lockfiles + `apps/api/data/emission_factors.json`
- `GREENLOGIX_FRONTIER_PLAN_2026-09-1.md` (ground truth, read-only)
- `FRONTIER_GAP_AUDIT_2026-09-21.md` + `2026-09-22.md` + `2026-09-23.md` + `2026-09-24.md` (priors untouched)
- Live secrets / API keys (never commit)

### Explicit exceptions

| Path | Allowed delta | Why |
|------|----------------|-----|
| `apps/api/openapi.json` | Additive only + regen via `dump_openapi` | 1–2 new admin paths; contract test updated |
| `apps/api/tests/test_contract.py` | `FROZEN_METHODS` +1/+2 entries | Allow new admin paths explicitly |
| `implementation-notes.md` | Append only | Running log per craftsmanship invariant |

## 3. Ownership

| Worktree | Owner | Type | Branch | Paths | Status |
|----------|-------|------|--------|-------|--------|
| WT-01 | loop-agent | ai | `so2026` (loop; see note) | `apps/api/src/greenlogix_api/routers/driver.py`, `schemas.py`, `openapi.json`, `tests/test_restriction_feedback.py`, `tests/test_contract.py` | Planned |

Branch note (needs integrator ack): rules say cut `cr/<nnn>-<scope>` from `origin/dev`, but this CR depends on CR-001's unmerged queue code (`geo/restrictions.py:128-186`, `routers/driver.py:132-160`). Options: (a) continue on `so2026` loop branch, or (b) new `cr/002-feedback-admin` cut from `so2026` instead of `dev`. Recommended: (a) — single-agent loop, additive-only, no parallel workers.

| Task | Description | Owner | WT | Depends | Status |
|------|-------------|-------|----|---------|--------|
| T-01 | Admin review endpoints (list + verify, manager-only) | loop-agent | WT-01 | — | Not Started |
| T-02 | Contract regen + tests + notes append | loop-agent | WT-01 | T-01 | Not Started |

**Hotspots this CR (one owner):**

| Path | Owner |
|------|-------|
| `apps/api/openapi.json` | WT-01 (additive regen only) |
| lockfiles | none (untouched) |

## 4. Acceptance

- [ ] **AC-01:** Manager lists pending queue and verifies entries via HTTP; scoped driver gets 403; unknown id gets 404
- [ ] **AC-02:** Existing tests/build for touched apps still pass (425+ green, zero regression)
- [ ] **AC-03:** No files outside MUST CHANGE / exceptions; `ACTIVE_RULES` byte-identical after verify flow

### Verify commands

```bash
cd apps/api && uv run pytest -q
# contract: test_frozen_openapi green (updated +1/+2 with regen note)
# banned claims: grep -rni "truck-safe\|ISO.*certified" src --include="*.py" | verify negations only
```

## 5. Integrator checklist (merge to `dev`)

- [ ] Diff `origin/dev...HEAD` contains no **MUST NOT TOUCH** paths
- [ ] Every **MUST CHANGE** item is done or explicitly cut
- [ ] **MUST KEEP** still holds (esp. no auto-mutate of `ACTIVE_RULES`)
- [ ] Verify commands ran (paste output summary)
- [ ] PR uses `feature-to-dev` template and links this file
- [ ] Decision: **approve** / **request changes** (cite CR lines)

Integrator notes: BLOCKED 2026-09-23 — user required fresh `cr/002-feedback-admin` from `origin/dev` (e47dbff). Worktree created per rules. But queue code (`geo/restrictions.py`, `POST /driver/restriction-feedback`) exists only on `so2026` (370900c, 21 commits ahead, incl. landing/slides/pitch-deck scope outside this CR). Fresh branch has no queue to build admin endpoints on. Options: (a) land CR-001 to dev first, then rebase this CR; (b) cherry-pick queue files only (violates surgical scope, drags auth/contract deps); (c) implement standalone queue in new branch (duplicates CR-001, merge conflict guaranteed). Recommend (a). Worktree `.worktrees/cr-002-feedback-admin` kept (empty, no code written).

## 6. Agent completion (each WT)

```yaml
cr_id: CR-20260923-002
task_ids: [T-01, T-02]
worktree_id: WT-01
branch: so2026
base_sha: "370900c"
final_sha: ""
files_changed: []
tests: [{ command: "", result: "" }]
unresolved_items: []
```

## 7. After merge

- [ ] CR moved to `changes/archive/`
- [ ] Worktrees removed
- [ ] PR URL:
