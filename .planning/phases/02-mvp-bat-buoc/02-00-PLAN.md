---
phase: 02-mvp-bat-buoc
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/greenlogix_api/late.py
  - apps/api/src/greenlogix_api/routers/orders.py
  - apps/api/src/greenlogix_api/routers/report.py
  - apps/api/src/greenlogix_api/schemas.py
  - apps/api/templates/dispatcher.html
  - apps/api/tests/test_late.py
  - apps/api/tests/test_report_xlsx.py
  - apps/api/openapi.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /orders?q=Q1 returns a subset; empty q returns all."
    - "GET /orders?late=1 returns only stops whose haversine ETA from depot at 30 km/h would miss window_end (no traffic API)."
    - "GET /report.xlsx is an openpyxl workbook with baseline vs optimized km/litres/kg_co2 and percent deltas."
    - "Dispatcher has search box, Late-only toggle, and Tải báo cáo xlsx; map stays above the fold."
  artifacts:
    - path: apps/api/src/greenlogix_api/late.py
      provides: haversine ETA vs window_end
      contains: late_risk
    - path: apps/api/tests/test_late.py
      provides: unit proof without clocks from traffic
      contains: window_end
---

## Objective

Chiến (API + dispatcher). Filter orders, flag late-risk with haversine×1.35 / 30 km/h vs window_end, export daily report xlsx. Zero paid APIs.

## Tasks

### T1 tests then late.py + GET /orders filters
- OrderOut grows `late_risk: bool`
- `q` matches address/phone/receiver case-insensitive
- `late=1` filters late_risk true
- Depot = first seed depot or (10.801, 106.661)
- ETA minutes = road_km / 30 * 60; compare to today's window_end

### T2 GET /report.xlsx
- Content-Type spreadsheetml
- Sheets or rows: metric, baseline, optimized, delta, delta_pct
- Same numbers as GET /report JSON

### T3 dispatcher
- Keep map+KPI layout
- Input#q, checkbox#late, button export opens /report.xlsx
- Filter calls GET /orders?q=&late=

Do not edit apps/landing or Flutter (that's 02-01). Atomic commits. uv pytest.
