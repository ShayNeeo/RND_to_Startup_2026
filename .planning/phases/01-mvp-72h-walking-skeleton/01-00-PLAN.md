---
phase: 01-mvp-72h-walking-skeleton
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - CR-20260902-001.md
  - .gitignore
  - apps/api/pyproject.toml
  - apps/api/.python-version
  - apps/api/README.md
  - apps/api/openapi.json
  - apps/api/src/greenlogix_api/__init__.py
  - apps/api/src/greenlogix_api/main.py
  - apps/api/src/greenlogix_api/auth.py
  - apps/api/src/greenlogix_api/db.py
  - apps/api/src/greenlogix_api/models.py
  - apps/api/src/greenlogix_api/schemas.py
  - apps/api/src/greenlogix_api/routers/__init__.py
  - apps/api/src/greenlogix_api/routers/orders.py
  - apps/api/src/greenlogix_api/routers/vehicles.py
  - apps/api/src/greenlogix_api/routers/optimize.py
  - apps/api/src/greenlogix_api/routers/driver.py
  - apps/api/src/greenlogix_api/routers/report.py
  - apps/api/tests/test_health.py
  - apps/api/tests/test_auth.py
  - apps/mobile-driver/pubspec.yaml
  - apps/mobile-driver/lib/main.dart
  - apps/mobile-driver/lib/api/client.dart
  - apps/mobile-driver/lib/models/health.dart
  - apps/mobile-driver/lib/models/driver_route.dart
  - apps/mobile-driver/lib/models/status_update.dart
  - apps/mobile-driver/lib/screens/pin_screen.dart
  - apps/mobile-driver/test/api_client_test.dart
  - apps/mobile-driver/README.md
autonomous: true
requirements:
  - COST-01
user_setup: []

must_haves:
  truths:
    - "Chiến's FastAPI answers GET /health with JSON status ok without any third-party geospatial credentials."
    - "Checked-in apps/api/openapi.json lists every frozen path so Thanh can hand-write Dart models (D-15)."
    - "Thanh's Flutter app is a real Flutter project under apps/mobile-driver (D-01, D-12), not the stale React PWA README stack."
    - "GREENLOGIX_DEMO=1 allows Bearer DEMO and X-Driver-Pin 0000; the same routes return 401 when the flag is unset (D-19)."
    - "Two worktrees exist: WT-01 API (Chiến) and WT-02 Flutter (Thanh) under CR-20260902-001 (D-01, D-02)."
  artifacts:
    - path: "CR-20260902-001.md"
      provides: "Change Request + WT-01/WT-02 registry"
      contains: "cr/001-api"
    - path: "apps/api/openapi.json"
      provides: "Frozen OpenAPI 3 contract (D-15)"
      contains: "/driver/route"
    - path: "apps/api/.python-version"
      provides: "Python 3.12 pin (D-10)"
      contains: "3.12"
    - path: "apps/api/src/greenlogix_api/main.py"
      provides: "FastAPI app, CORS, lifespan, router include"
      contains: "GREENLOGIX_DEMO"
    - path: "apps/mobile-driver/lib/api/client.dart"
      provides: "Dart HTTP client with API_BASE dart-define"
      contains: "X-Driver-Pin"
    - path: "apps/mobile-driver/lib/models/driver_route.dart"
      provides: "Hand-written Dart models from openapi.json"
      contains: "DriverStop"
  key_links:
    - from: "apps/mobile-driver/lib/api/client.dart"
      to: "apps/api/src/greenlogix_api/main.py"
      via: "GET /health then later GET /driver/route"
      pattern: "/health"
    - from: "apps/api/src/greenlogix_api/auth.py"
      to: "apps/api/src/greenlogix_api/routers/driver.py"
      via: "require_driver dependency on X-Driver-Pin"
      pattern: "X-Driver-Pin"
---

## Phase Goal

**As a** HCMC dispatcher, **I want to** import today's Excel orders, get clustered routes that beat the old sequence on km and CO₂, and have tài xế execute the list in Flutter with a Maps deep-link, **so that** a contest jury sees dual value in one sitting.

## People split

| Task | owner | hour_box | Worktree | Branch |
|------|-------|----------|----------|--------|
| 1 | chien | 1h | WT-01 `../repo-cr001-api` (creates both WT rows) | `cr/001-api` |
| 2 | chien | 3h | WT-01 `../repo-cr001-api` | `cr/001-api` |
| 3 | thanh | 3h | WT-02 `../repo-cr001-flutter` | `cr/001-flutter` |

Wall clock hours 0–4 (D-03). Chiến 4h. Thanh flutter create may overlap T2; Dart models wait on sibling `../repo-cr001-api/apps/api/openapi.json` after T2 commit. Do not edit `apps/landing/**` or `apps/web-portal/**`.

<objective>
Freeze the only shared contract (D-15) and the two-person file ownership seam so Wave 1 can implement without renaming fields.

Purpose: Hour 0–4 contract freeze from 01-RESEARCH.md. Thanh must GET /health from a Flutter `http` client against Chiến's uv FastAPI (D-01, D-02, D-10, D-12, D-19). COST-01 starts here: the scaffold introduces zero commercial geospatial credentials.

Output: CR-20260902-001.md, apps/api FastAPI skeleton + checked-in openapi.json, Flutter project with hand-written Dart models, both worktrees.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md
@README.md
@apps/mobile-driver/README.md
@$HOME/.agents/skills/change-request-management/SKILL.md
@$HOME/.agents/skills/python-development-uv-package-manager/SKILL.md
</context>

<interfaces>
Frozen HTTP surface (D-15). Chiến publishes these path operations in openapi.json this plan. Bodies may return empty lists / 503 until 01-01, but the path+method+header contract is frozen. Breaking field renames after hour 4 require a CR ping.

Auth (D-19):
- Dispatcher routes: header Authorization: Bearer DEMO
- Driver routes: header X-Driver-Pin: 0000
- Env GREENLOGIX_DEMO=1 required or every listed route except GET /health and GET /openapi.json returns 401
- Query ?plate=51C-000.03 on GET /driver/route is allowed (filter); omitting it returns all published routes grouped by plate

| Method | Path | Auth |
|--------|------|------|
| GET | /health | none |
| GET | /openapi.json | none |
| GET | /dispatcher | Bearer DEMO |
| POST | /seed | Bearer DEMO |
| POST | /orders/import | Bearer DEMO (multipart file) |
| GET | /orders | Bearer DEMO |
| PATCH | /orders/{id} | Bearer DEMO |
| DELETE | /orders/{id} | Bearer DEMO |
| GET | /vehicles | Bearer DEMO |
| PATCH | /vehicles/{id} | Bearer DEMO |
| POST | /optimize | Bearer DEMO |
| GET | /routes | Bearer DEMO |
| POST | /routes/publish | Bearer DEMO |
| GET | /report | Bearer DEMO |
| GET | /driver/route | X-Driver-Pin |
| POST | /stops/{id}/status | X-Driver-Pin |
| POST | /stops/{id}/photo | X-Driver-Pin |

JSON keys (Pydantic / Dart must use these names, not aliases):

HealthOut: status (string, value ok)

OrderOut: id (int), address (str), lat (float), lng (float), receiver (str), phone (str), kg (float), window_start (str HH:MM), window_end (str HH:MM), cargo_type (str: thuong\|lanh\|de_vo), notes (str), excel_row (int\|null), status (str: pending\|assigned\|arrived\|delivered\|failed)

OrderPatch: any subset of address, lat, lng, receiver, phone, kg, window_start, window_end, cargo_type, notes

ImportResult: imported (int), errors (list of {excel_row:int, field:str, message:str})

VehicleOut: id (int), plate (str), type (str), capacity_kg (float), fuel (str: petrol\|diesel), l_per_100km (float), status (str: ready\|maintenance)

VehiclePatch: type, capacity_kg, fuel, l_per_100km, status

OptimizeIn: cluster_radius_km (float, default 3.0)

StopOut: id (int), seq (int), kind (str: depot\|stop), order_id (int\|null), lat (float), lng (float), address (str), phone (str), window_start (str), window_end (str), notes (str), kg (float), status (str: pending\|arrived\|delivered\|failed), fail_reason (str\|null)

RouteOut: id (int), vehicle_id (int), plate (str), color (str), published (bool), km (float), litres (float), kg_co2 (float), overload (bool), stops (list StopOut)

OptimizeOut: routes (list RouteOut), unassigned_order_ids (list int), totals (object km, litres, kg_co2)

PublishIn: route_ids (list int). Empty list means publish all current unpublished routes.

DriverRouteOut: plate (str), stops (list StopOut) — driver payload omits unpublished routes

DriverRouteList: routes (list DriverRouteOut)

StatusIn: status (str: arrived\|delivered\|failed), reason (str\|null; enum khach_vang\|sai_dia_chi\|hang_hong\|tu_choi; required when status=failed)

StatusOut: id (int), status (str), reason (str\|null)

ReportTotals: km (float), litres (float), kg_co2 (float)

ReportOut: baseline (ReportTotals), optimized (ReportTotals), delta (object km, litres, kg_co2, km_pct, litres_pct, kg_co2_pct)

SeedOut: orders (int), vehicles (int), depot (object lat, lng, name)

CORS (demo, D-19): CORSMiddleware allow_origins=["*"], allow_credentials=False, allow_methods=["*"], allow_headers=["*"]. Native Flutter ignores CORS; this is for flutter run -d chrome only.

Uvicorn: --host 0.0.0.0 --port 8000 (emulator 10.0.2.2 needs this bind).
</interfaces>

<artifacts_produced>
New files this plan: CR-20260902-001.md; apps/api/{pyproject.toml,.python-version,openapi.json,README.md,src/greenlogix_api/**,tests/test_health.py,tests/test_auth.py}; Flutter lib/{main.dart,api/client.dart,models/*.dart,screens/pin_screen.dart}.

CLI: git worktree add; uv python pin 3.12; uv init --name greenlogix-api --package; uv add fastapi "uvicorn[standard]" sqlmodel openpyxl jinja2 python-multipart; uv add --dev pytest ruff; flutter create --org vn.greenlogix --project-name mobile_driver --platforms=android,ios,linux .; flutter pub add http url_launcher; GREENLOGIX_DEMO=1 uv run uvicorn greenlogix_api.main:app --host 0.0.0.0 --port 8000.

Env: GREENLOGIX_DEMO=1 only. No commercial geospatial keys.

Dart classes: ApiClient, HealthOut, DriverStop, DriverRoute, DriverRouteList, StatusIn, PinScreen.

FastAPI routes: all frozen paths registered (empty/503 bodies allowed except /health).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write CR-20260902-001 and create WT-01 / WT-02 worktrees</name>
  <owner>chien</owner>
  <hour_box>1h</hour_box>
  <files>CR-20260902-001.md</files>
  <read_first>.planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md, /home/shayneeo/.agents/skills/change-request-management/SKILL.md, README.md</read_first>
  <reversibility rating="costly">Two builders share this CR; changing worktree paths after T2/T3 start forces a rebase of both branches.</reversibility>
  <action>
    Create CR-20260902-001.md at repo root using the change-request/v1 schema from the change-request-management skill (D-01, D-02). Title: 72h walking skeleton API + Flutter. status: in_progress. base_branch: current default branch.

    Worktree registry (exact paths from 01-RESEARCH.md Parallel file ownership):
    - WT-01 owner Chiến (Nguyễn Quang Chiến), Human, path ../repo-cr001-api, branch cr/001-api, scope apps/api/** plus apps/api/openapi.json plus CR file plus root .gitignore API lines.
    - WT-02 owner Thanh (Phạm Quốc Thanh), Human, path ../repo-cr001-flutter, branch cr/001-flutter, scope apps/mobile-driver/** only.

    File ownership table must state: Chiến exclusive on apps/api/**; Thanh exclusive on apps/mobile-driver/**; openapi.json Chiến-write Thanh-read after hour 4; apps/landing/** and apps/web-portal/** nobody this phase (D-13); root README.md demo section is Chiến in plan 01-03.

    Task table: T-01 this CR, T-02 FastAPI OpenAPI freeze, T-03 Flutter scaffold. Commit trailer format: CR: CR-20260902-001 / Task: T-XX / Worktree: WT-XX.

    Then from repo root: git fetch if a remote exists; git worktree add -b cr/001-api ../repo-cr001-api HEAD; git worktree add -b cr/001-flutter ../repo-cr001-flutter HEAD. If worktrees already exist at those paths, reuse them and record the SHA in the CR. Do not use npm or yarn. Do not create Dockerfiles.
  </action>
  <verify>
    <automated>test -f CR-20260902-001.md && grep -F 'cr/001-api' CR-20260902-001.md && grep -F 'cr/001-flutter' CR-20260902-001.md && grep -F '../repo-cr001-api' CR-20260902-001.md && grep -F 'apps/mobile-driver' CR-20260902-001.md && git worktree list | grep -E 'cr/001-api|cr/001-flutter'</automated>
  </verify>
  <acceptance_criteria>
    - CR-20260902-001.md exists and contains strings cr/001-api, cr/001-flutter, WT-01, WT-02
    - git worktree list shows both branches
    - CR file ownership table assigns apps/api/** to Chiến and apps/mobile-driver/** to Thanh (D-01, D-02)
  </acceptance_criteria>
  <done>CR is on disk and both worktrees exist so T2 and T3 can proceed in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Python 3.12, scaffold FastAPI, dump openapi.json, demo auth</name>
  <owner>chien</owner>
  <hour_box>3h</hour_box>
  <files>apps/api/pyproject.toml, apps/api/.python-version, apps/api/src/greenlogix_api/main.py, apps/api/src/greenlogix_api/auth.py, apps/api/src/greenlogix_api/db.py, apps/api/src/greenlogix_api/models.py, apps/api/src/greenlogix_api/schemas.py, apps/api/src/greenlogix_api/routers/__init__.py, apps/api/src/greenlogix_api/routers/orders.py, apps/api/src/greenlogix_api/routers/vehicles.py, apps/api/src/greenlogix_api/routers/optimize.py, apps/api/src/greenlogix_api/routers/driver.py, apps/api/src/greenlogix_api/routers/report.py, apps/api/openapi.json, apps/api/tests/test_health.py, apps/api/tests/test_auth.py, .gitignore, apps/api/README.md</files>
  <read_first>.planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md, /home/shayneeo/.agents/skills/python-development-uv-package-manager/SKILL.md</read_first>
  <reversibility rating="costly">openapi.json is the D-15 contract Thanh copies into Dart; renaming paths after hour 4 is a coordinated CR change.</reversibility>
  <action>
    Work in WT-01 ../repo-cr001-api (D-02, D-10, D-14). mkdir -p apps/api. Inside apps/api: uv python pin 3.12 (writes .python-version containing 3.12). Do not use the system 3.14 interpreter. uv init --name greenlogix-api --package if pyproject.toml is absent. Set requires-python to 3.12.x only (greater or equal 3.12, less than 3.13).

    uv add fastapi "uvicorn[standard]" sqlmodel openpyxl jinja2 python-multipart. uv add --dev pytest ruff. Do not uv add httpx (Nominatim unwired). Do not uv add ortools (D-16 default path). Do not pip install. Package legitimacy: FastAPI 0.141.x, uvicorn 0.52.x, sqlmodel 0.0.42, openpyxl 3.1.5, jinja2 3.1.6, python-multipart 0.0.32, pytest 9.x, ruff 0.16.x as in 01-RESEARCH.md Standard Stack — already Approved; no human package checkpoint.

    Implement apps/api/src/greenlogix_api/main.py: FastAPI title GreenLogix API; lifespan creates data dir apps/api/data; CORSMiddleware allow_origins star, allow_credentials False, allow_methods star, allow_headers star (D-19). Include routers for orders, vehicles, optimize, driver, report.

    auth.py: function require_dispatcher reads Authorization Bearer DEMO only when os.environ GREENLOGIX_DEMO equals 1, else HTTPException 401. function require_driver reads header X-Driver-Pin equal to 0000 under the same flag (D-19). GET /health and the OpenAPI docs routes stay public.

    Register every frozen path from the interfaces table. Until plan 01-01, non-health handlers may return empty JSON matching the schema names (lists empty, numbers 0) or HTTP 503 with detail not_implemented — they MUST appear in app.openapi() paths. GET /health returns {"status":"ok"}. GET /dispatcher may return a one-line HTML 200 "GreenLogix dispatcher" so the path exists.

    db.py: create_engine sqlite URL pointing at apps/api/data/greenlogix.db with connect_args check_same_thread False (D-11). create_all on lifespan. models.py may declare empty SQLModel subclasses named Order, Vehicle, Route, Stop with table=True and the columns listed in interfaces (id PK) so OpenAPI component schemas match. Do not invent a third contract in packages/shared-types (D-15).

    Dump apps/api/openapi.json via app.openapi() written with indent 2. Commit that file.

    schemas.py: request/response models matching the interfaces table JSON keys (HealthOut, OrderOut, OrderPatch, ImportResult, VehicleOut, VehiclePatch, OptimizeIn, StopOut, RouteOut, OptimizeOut, PublishIn, DriverRouteOut, DriverRouteList, StatusIn, StatusOut, ReportTotals, ReportOut, SeedOut).

    tests/test_health.py: TestClient GET /health status_code 200 and JSON status ok; TestClient GET /openapi.json status_code 200 (the live docs URL is public; FastAPI sets include_in_schema False for that URL so it must not be asserted inside app.openapi()["paths"]). tests/test_auth.py: with GREENLOGIX_DEMO unset or 0, GET /orders returns 401; with GREENLOGIX_DEMO=1 and Authorization Bearer DEMO, GET /orders returns 200 or 503 not 401; GET /driver/route without X-Driver-Pin returns 401.

    Append to repo .gitignore: apps/api/data/greenlogix.db, apps/api/data/uploads/, apps/api/.venv/, **/__pycache__/, *.pyc. Do not gitignore openapi.json, emission factor JSON, or seed xlsx.

    apps/api/README.md: how to run GREENLOGIX_DEMO=1 uv run uvicorn greenlogix_api.main:app --host 0.0.0.0 --port 8000. Do not document commercial map keys.

    Logging: log route path and order id only. Do not interpolate phone numbers into log messages (ASVS L1).
  </action>
  <verify>
    <automated>cd apps/api && test "$(cat .python-version | tr -d ' \n')" = "3.12" && test -f openapi.json && uv run pytest -x tests/test_health.py tests/test_auth.py && python3 -c "import json,sys; p=json.load(open('openapi.json')); paths=p['paths']; req=['/health','/dispatcher','/seed','/orders/import','/orders','/orders/{id}','/vehicles','/vehicles/{id}','/optimize','/routes','/routes/publish','/report','/driver/route','/stops/{id}/status','/stops/{id}/photo']; missing=[x for x in req if x not in paths]; sys.exit('missing '+str(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/.python-version equals 3.12 (D-10)
    - apps/api/openapi.json file exists and paths include /health, /driver/route, /optimize, /orders/import, /stops/{id}/status, /stops/{id}/photo (D-15). Do not require /openapi.json inside the dumped paths object (FastAPI include_in_schema False).
    - TestClient GET /openapi.json returns 200 (D-15, public docs URL)
    - uv run pytest -x tests/test_health.py tests/test_auth.py exits 0 (D-19)
    - pyproject.toml dependencies include fastapi and sqlmodel; they do not include ortools or httpx
    - .gitignore contains apps/api/data/greenlogix.db
  </acceptance_criteria>
  <done>Thanh can read a committed openapi.json and curl GET /health on port 8000 with GREENLOGIX_DEMO=1.</done>
</task>

<task type="auto">
  <name>Task 3: Flutter create, dart-define API_BASE, hand-write models, PIN screen hits /health</name>
  <owner>thanh</owner>
  <hour_box>3h</hour_box>
  <precondition>Sibling worktree dump ../repo-cr001-api/apps/api/openapi.json exists after Task 2 commit and its paths object contains /health. WT-02 ../repo-cr001-flutter is on branch cr/001-flutter. flutter create may overlap Task 2; Dart model files must not.</precondition>
  <files>apps/mobile-driver/pubspec.yaml, apps/mobile-driver/lib/main.dart, apps/mobile-driver/lib/api/client.dart, apps/mobile-driver/lib/models/health.dart, apps/mobile-driver/lib/models/driver_route.dart, apps/mobile-driver/lib/models/status_update.dart, apps/mobile-driver/lib/screens/pin_screen.dart, apps/mobile-driver/test/api_client_test.dart, apps/mobile-driver/README.md</files>
  <read_first>../repo-cr001-api/apps/api/openapi.json, apps/mobile-driver/README.md, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md</read_first>
  <action>
    Work in WT-02 ../repo-cr001-flutter only (D-01, D-12). Copy the current apps/mobile-driver/README.md aside (it describes a React PWA — D-12 overrides it). From apps/mobile-driver run flutter create --org vn.greenlogix --project-name mobile_driver --platforms=android,ios,linux . so the existing folder becomes a Flutter app. flutter create may overlap Chiến Task 2. flutter pub add http url_launcher. Do not add maps_launcher. Do not add image_picker yet (that is plan 01-03). Do not implement the old PWA stack.

    Contract copy path (D-15): after Task 2 commits, read OpenAPI from the sibling API worktree file ../repo-cr001-api/apps/api/openapi.json (WT-02 cwd is ../repo-cr001-flutter, so that relative path is the Chiến dump). Do not hand-write Dart models until that sibling file exists. Do not wait on a copy inside WT-02; the API worktree is the source of truth.

    Layout (discretion lock from 01-RESEARCH.md): lib/api/client.dart, lib/models/, lib/screens/.

    ApiClient: constructor reads const String.fromEnvironment('API_BASE', defaultValue: 'http://127.0.0.1:8000'). Methods: getHealth() GET /health; getDriverRoute({String? plate}) GET /driver/route with header X-Driver-Pin from the PIN the user typed; later plans add postStatus. Timeout 15s. Never log the phone field from JSON.

    Hand-write Dart classes from ../repo-cr001-api/apps/api/openapi.json (D-15). Do not run openapi-generator. Required classes: HealthOut with status; DriverStop with id, seq, kind, orderId mapped from order_id, lat, lng, address, phone, windowStart from window_start, windowEnd from window_end, notes, kg, status, failReason from fail_reason; DriverRoute with plate and a list of DriverStop named stops; DriverRouteList with a list of DriverRoute named routes; StatusIn with status and reason. Use json keys exactly as in the OpenAPI schema (snake_case on the wire).

    PinScreen: TextField obscureText, button Đăng nhập. On submit, call getHealth() then GET /driver/route with header X-Driver-Pin set to the typed value. PIN 0000 is the demo value (D-19). Non-200 shows the status code. Do not hardcode a fake stop list — an empty routes array from the API is the correct hour-4 state.

    Replace apps/mobile-driver/README.md with Flutter run instructions: flutter run --dart-define=API_BASE=http://127.0.0.1:8000 -d linux; emulator uses http://10.0.2.2:8000; physical phone uses the LAN IP of the host. Document that uvicorn must bind 0.0.0.0.

    Add a widget or unit test test/api_client_test.dart that parses a fixture JSON {"routes":[{"plate":"51C-000.01","stops":[{"id":1,"seq":1,"kind":"stop","order_id":1,"lat":10.776,"lng":106.7,"address":"Q1","phone":"0900000001","window_start":"08:00","window_end":"11:00","notes":"","kg":10,"status":"pending","fail_reason":null}]}]} into DriverRouteList and expects plate 51C-000.01.
  </action>
  <verify>
    <automated>test -f apps/mobile-driver/pubspec.yaml && grep -E 'http:|url_launcher' apps/mobile-driver/pubspec.yaml && grep -F 'API_BASE' apps/mobile-driver/lib/api/client.dart && grep -F 'X-Driver-Pin' apps/mobile-driver/lib/api/client.dart && grep -F 'DriverStop' apps/mobile-driver/lib/models/driver_route.dart && grep -F 'window_start' apps/mobile-driver/lib/models/driver_route.dart && (cd apps/mobile-driver && flutter test test/api_client_test.dart && flutter analyze --no-fatal-infos)</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml lists http and url_launcher and does not list maps_launcher (D-08, D-12)
    - lib/api/client.dart contains API_BASE and X-Driver-Pin (D-19)
    - lib/models/driver_route.dart contains DriverStop and JSON key window_start (D-15)
    - flutter test test/api_client_test.dart exits 0
    - apps/mobile-driver/README.md describes flutter run and dart-define API_BASE, not a React PWA
  </acceptance_criteria>
  <done>Thanh has a Flutter app that compiles, parses the frozen driver JSON, and can GET /health against Chiến's API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter → FastAPI | PIN header and later PII (phone, address) cross here |
| Browser → FastAPI | Bearer DEMO and xlsx uploads (later) |
| FastAPI process → disk | SQLite file and future POD photos |
| Public OpenAPI | No secrets; demo flag still required for data routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-00-01 | Elevation | auth.py require_dispatcher / require_driver | high | mitigate | Return 401 unless GREENLOGIX_DEMO=1 and token/PIN match; tests/test_auth.py covers the off-flag path (D-19) |
| T-01-00-02 | Information disclosure | routers + logging | medium | mitigate | Log order id and path only; never interpolate phone into logger calls |
| T-01-00-03 | Spoofing | CORSMiddleware | low | mitigate | allow_credentials False with wildcard origins so Authorization is not combined with credentials |
| T-01-00-04 | Information disclosure | demo PIN 0000 | high | accept | Contest localhost demo per D-19; README states localhost-only; not a product IdP |
| T-01-00-05 | Tampering | SQLModel | medium | mitigate | Parameterized SQLModel only; no f-string SQL |
| T-01-SC | Tampering | uv/pub installs | high | mitigate | Install only RESEARCH.md Approved packages (fastapi, uvicorn, sqlmodel, openpyxl, jinja2, python-multipart, pytest, ruff, http, url_launcher). Do not add maps_launcher or ortools |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] git worktree list shows cr/001-api and cr/001-flutter
- [ ] cd apps/api && uv run pytest -x tests/test_health.py tests/test_auth.py
- [ ] apps/api/openapi.json contains /driver/route and /optimize
- [ ] cd apps/mobile-driver && flutter test test/api_client_test.dart
- [ ] GET /health with uvicorn on 0.0.0.0:8000 returns {"status":"ok"}
</verification>

<success_criteria>
- D-15 contract file is committed and lists the frozen paths
- D-10 Python 3.12 pin is real (.python-version)
- D-01/D-02 worktrees isolate Flutter vs API
- D-19 demo auth rejects requests when GREENLOGIX_DEMO is off
- COST-01: scaffold env samples contain only GREENLOGIX_DEMO (and optional TZ)
- Hour box this plan: Chiến 4h, Thanh 3h
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-72h-walking-skeleton/01-00-SUMMARY.md`
</output>
