---
phase: 01-mvp-72h-walking-skeleton
plan: 01
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - apps/api/src/greenlogix_api/main.py
  - apps/api/src/greenlogix_api/db.py
  - apps/api/src/greenlogix_api/models.py
  - apps/api/src/greenlogix_api/schemas.py
  - apps/api/src/greenlogix_api/seed.py
  - apps/api/src/greenlogix_api/carbon.py
  - apps/api/src/greenlogix_api/ingest_xlsx.py
  - apps/api/src/greenlogix_api/solver/distance.py
  - apps/api/src/greenlogix_api/solver/cluster.py
  - apps/api/src/greenlogix_api/solver/nn_two_opt.py
  - apps/api/src/greenlogix_api/solver/baseline.py
  - apps/api/src/greenlogix_api/routers/orders.py
  - apps/api/src/greenlogix_api/routers/vehicles.py
  - apps/api/src/greenlogix_api/routers/optimize.py
  - apps/api/src/greenlogix_api/routers/driver.py
  - apps/api/templates/dispatcher.html
  - apps/api/data/emission_factors.json
  - apps/api/data/seed/hcmc_80_orders.xlsx
  - apps/api/data/seed/hcmc_10_trucks.xlsx
  - apps/api/openapi.json
  - apps/api/tests/test_distance.py
  - apps/api/tests/test_solver.py
  - apps/api/tests/test_carbon.py
  - apps/api/tests/test_baseline.py
  - apps/api/tests/test_tracer_e2e.py
  - apps/api/tests/test_ingest.py
  - apps/mobile-driver/lib/api/client.dart
  - apps/mobile-driver/lib/screens/pin_screen.dart
  - apps/mobile-driver/lib/screens/route_list.dart
  - apps/mobile-driver/lib/models/driver_route.dart
  - apps/mobile-driver/test/route_list_test.dart
autonomous: true
requirements:
  - ORD-01
  - ORD-02
  - VEH-01
  - VEH-02
  - VRP-01
  - VRP-02
  - VRP-03
  - VRP-04
  - DSP-01
  - DSP-02
  - DRV-01
  - RPT-01
user_setup: []

must_haves:
  truths:
    - "One command GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed loads 80 inner-HCMC orders and 10 trucks (ORD-02, D-18)."
    - "Dispatcher HTML at GET /dispatcher: seed or upload, Optimize, Leaflet OSM polylines, Publish (DSP-01, DSP-02, D-05)."
    - "POST /optimize clusters then NN+2-opt with haversine times HCMC_CIRCUITY 1.35, ready vehicles only, capacity flagged, depot-stop-depot (VRP-01..04, VEH-02, D-07, D-16)."
    - "After Publish, Flutter PIN 0000 shows the ordered stop list with address, phone, window, notes (DRV-01, D-01, D-19)."
    - "Dispatcher shows computed km, litres, kg CO2 from in-repo emission_factors.json after optimize (RPT-01, D-09)."
  artifacts:
    - path: "apps/api/data/seed/hcmc_80_orders.xlsx"
      provides: "80-order HCMC seed with lat/lng (D-18)"
    - path: "apps/api/data/seed/hcmc_10_trucks.xlsx"
      provides: "10-truck seed 500kg–2t (VEH-01)"
    - path: "apps/api/data/emission_factors.json"
      provides: "TTW kg_co2_per_litre petrol 2.31 diesel 2.68 (D-09)"
      contains: "tank_to_wheel"
    - path: "apps/api/src/greenlogix_api/solver/distance.py"
      provides: "haversine and HCMC_CIRCUITY (D-07, VRP-03)"
      contains: "HCMC_CIRCUITY"
    - path: "apps/api/templates/dispatcher.html"
      provides: "Jinja Leaflet OSM dispatcher (D-13, D-05)"
      contains: "tile.openstreetmap.org"
    - path: "apps/mobile-driver/lib/screens/route_list.dart"
      provides: "Ordered stop list UI (DRV-01)"
      contains: "window_start"
  key_links:
    - from: "apps/mobile-driver/lib/screens/route_list.dart"
      to: "apps/api/src/greenlogix_api/routers/driver.py"
      via: "GET /driver/route with X-Driver-Pin 0000"
      pattern: "/driver/route"
    - from: "apps/api/src/greenlogix_api/routers/optimize.py"
      to: "apps/api/data/greenlogix.db"
      via: "SQLModel Session commit of Route and Stop rows"
      pattern: "Session"
    - from: "apps/api/templates/dispatcher.html"
      to: "apps/api/src/greenlogix_api/routers/optimize.py"
      via: "fetch POST /optimize then Leaflet polyline from routes[].stops lat/lng"
      pattern: "/optimize"
---

## Phase Goal

**As a** HCMC dispatcher, **I want to** import today's Excel orders, get clustered routes that beat the old sequence on km and CO₂, and have tài xế execute the list in Flutter with a Maps deep-link, **so that** a contest jury sees dual value in one sitting.

## People split

| Task | owner | hour_box | Worktree | Branch |
|------|-------|----------|----------|--------|
| 1 tracer | chien | 14h | WT-01 `../repo-cr001-api` | `cr/001-api` |
| 2 | thanh | 16h | WT-02 `../repo-cr001-flutter` | `cr/001-flutter` |
| 3 | chien | 6h | WT-01 `../repo-cr001-api` | `cr/001-api` |

Wall clock hours 4–24 (D-03). Task 2 starts when GET /driver/route returns at least one published stop. Task 3 can overlap Task 2.

<objective>
Prove the walking-skeleton architecture on one path: Excel/seed → SQLite → cluster+NN+2-opt → Jinja Leaflet OSM → Publish → Flutter ordered list (ORD-02, VRP-01..04, DSP-01/02, DRV-01, RPT-01, D-05, D-07, D-09, D-11, D-13, D-16, D-17, D-18).

Purpose: Tracer-first. Stubs allowed only where 01-02 can fill them without changing tables or paths (status buttons and Maps deep-link are 01-02).

Output: Seed xlsx, solver, dispatcher map, Flutter list showing published stops.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-00-PLAN.md
@apps/api/openapi.json
</context>

<artifacts_produced>
New/filled this plan: apps/api/data/seed/hcmc_80_orders.xlsx, hcmc_10_trucks.xlsx, emission_factors.json; solver/*.py; carbon.py; ingest_xlsx.py; seed.py; templates/dispatcher.html; tests/test_{distance,solver,carbon,baseline,ingest}.py; Flutter route_list.dart.

CLI: GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed ; curl POST /optimize ; curl POST /routes/publish ; curl GET /driver/route.

Env: GREENLOGIX_DEMO=1. TZ=Asia/Ho_Chi_Minh documented as naive local, no pytz.

JSON keys: same as 01-00 interfaces. OptimizeIn.cluster_radius_km default 3.0. RouteOut.overload bool. Report totals on dispatcher even if GET /report HTML polish waits for 01-03 — RPT-01 numbers must appear on the dispatcher page (keys km, litres, kg_co2).

Dart: RouteListScreen.

FastAPI: POST /seed, POST /optimize, POST /routes/publish, GET /routes, GET /dispatcher, GET /driver/route, POST /orders/import, GET/PATCH /vehicles, GET /orders implemented for real.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>End-to-end seed → optimize → Leaflet OSM → publish → GET /driver/route</name>
  <owner>chien</owner>
  <hour_box>14h</hour_box>
  <precondition>Plan 01-00 dumped apps/api/openapi.json with /optimize and /driver/route; apps/api/.python-version is 3.12; GREENLOGIX_DEMO auth helpers exist.</precondition>
  <reversibility rating="costly">SQLite table shapes (Order, Vehicle, Route, Stop) and HCMC_CIRCUITY are read by every later plan; column renames need a coordinated OpenAPI bump (D-07, D-11, D-15).</reversibility>
  <files>apps/api/src/greenlogix_api/main.py, apps/api/src/greenlogix_api/models.py, apps/api/src/greenlogix_api/db.py, apps/api/src/greenlogix_api/seed.py, apps/api/src/greenlogix_api/carbon.py, apps/api/src/greenlogix_api/solver/distance.py, apps/api/src/greenlogix_api/solver/cluster.py, apps/api/src/greenlogix_api/solver/nn_two_opt.py, apps/api/src/greenlogix_api/solver/baseline.py, apps/api/src/greenlogix_api/routers/optimize.py, apps/api/src/greenlogix_api/routers/driver.py, apps/api/templates/dispatcher.html, apps/api/data/emission_factors.json, apps/api/data/seed/hcmc_80_orders.xlsx, apps/api/data/seed/hcmc_10_trucks.xlsx, apps/api/tests/test_distance.py, apps/api/tests/test_solver.py, apps/api/tests/test_carbon.py, apps/api/tests/test_baseline.py, apps/api/tests/test_tracer_e2e.py, apps/api/openapi.json</files>
  <read_first>apps/api/src/greenlogix_api/main.py, apps/api/src/greenlogix_api/auth.py, apps/api/openapi.json, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md</read_first>
  <behavior>
    - road_km(lat1,lng1,lat2,lng2) equals haversine_km times HCMC_CIRCUITY 1.35 (D-07, VRP-03)
    - optimize ignores Vehicle.status != ready (VEH-02)
    - if assigned kg sum exceeds capacity_kg the route has overload true and the overflowing orders appear in unassigned_order_ids (VRP-02)
    - every Route.stops starts and ends with kind depot (VRP-04)
    - NN tie-break uses window_start ascending (VRP-04)
    - baseline fills vehicles in spreadsheet excel_row order until capacity, sequence unchanged (D-17)
    - kg_co2 = road_km * (l_per_100km/100) * kg_co2_per_litre; diesel 2.68, petrol 2.31 (D-09, RPT-01)
    - POST /seed inserts 80 orders and 10 vehicles including at least one maintenance truck (ORD-02, VEH-01, D-18)
  </behavior>
  <action>
    Stay in WT-01 apps/api/** (D-02). Do not edit apps/mobile-driver. Do not add Next.js under apps/web-portal (D-13). Do not call Nominatim (D-06 discretion: seed already has lat/lng). Do not add sklearn, numpy, pandas, geopy, or ortools (D-16). Use math for haversine.

    SQLModel tables in models.py (D-11): Order, Vehicle, Route, Stop with columns matching 01-00 JSON keys plus Route.published bool default false, Stop.route_id FK, Vehicle.status ready|maintenance. SQLite file apps/api/data/greenlogix.db. connect_args check_same_thread False.

    emission_factors.json exactly as 01-RESEARCH.md Emission factors block: boundary tank_to_wheel, fuels.petrol.kg_co2_per_litre 2.31, fuels.diesel.kg_co2_per_litre 2.68, aliases xăng/gasoline/petrol and dầu/DO/diesel, HCMC_CIRCUITY 1.35, note that this is a contest TTW estimate not an ISO 14083 pack (D-09).

    distance.py: named constant HCMC_CIRCUITY = 1.35 (D-07). haversine_km and road_km. Same factor on baseline and optimized.

    cluster.py: greedy radius on lat/lng, default CLUSTER_RADIUS_KM = 3.0, overridable by OptimizeIn.cluster_radius_km (VRP-01).

    nn_two_opt.py: nearest neighbor from depot (10.801, 106.661 Tân Bình — D-18 freeze), window_start as NN tie-break, then 2-opt cap 500 iterations or first local minimum, hard stop 2000 swaps (discretion). Close depot → stops → depot (VRP-04).

    After clustering, assign to ready vehicles only (VEH-02). If cluster kg sum exceeds capacity_kg, split or leave extras in unassigned_order_ids and set overload true on any infeasible assignment — never drop the flag (VRP-02).

    baseline.py (D-17): walk orders by excel_row ascending, greedy-fill ready vehicles by capacity_kg, keep spreadsheet sequence. Compute km/litres/kg_co2 with the same road_km and factors. Persist baseline totals for GET /report (01-03 displays the comparison; this task must compute and store them so RPT-02 is not hardcoded later).

    carbon.py: load emission_factors.json; resolve fuel aliases; formula kg_co2 = km * (l_per_100km/100.0) * kg_co2_per_litre (RPT-01, D-09).

    Seed files (ORD-02, D-18, VEH-01):
    - Depot lat 10.801 lng 106.661 name Tan Binh DC stored in seed.py as DEPOT_LAT DEPOT_LNG.
    - 80 orders spread across Q1 (~10.776,106.700), Q3 (~10.782,106.686), Binh Thanh (~10.810,106.709), Phu Nhuan (~10.799,106.675), Q7 (~10.729,106.721), Thu Duc (~10.850,106.772) with ~0.008 deg jitter so Leaflet is not a single blob.
    - Phones fictional 0900000001–0900000080 (Nghị định 13 — no real customer phones).
    - kg 5–80 so 500 kg vans can fill.
    - window_start/end naive HH:MM Asia/Ho_Chi_Minh, no pytz.
    - Write rows in zig-zag district order so the D-17 baseline km is worse than optimized; do not hardcode marketing percents (A4).
    - 10 trucks plates 51C-000.01–51C-000.10, type xe_tai_nho, capacity_kg mix 500,800,1000,1500,2000, fuel petrol or diesel with l_per_100km 8–14, nine ready, one maintenance (VEH-02 proof).
    - openpyxl write to apps/api/data/seed/hcmc_80_orders.xlsx and hcmc_10_trucks.xlsx. Headers bilingual-capable: address, lat, lng, receiver, phone, kg, window_start, window_end, cargo_type, notes and plate, type, capacity_kg, fuel, l_per_100km, status.

    seed.py module: GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed creates the db, inserts depot constant usage, 80 orders, 10 vehicles. POST /seed does the same (D-19 Bearer DEMO).

    POST /optimize: run cluster+NN+2-opt+baseline, persist Route/Stop, return OptimizeOut including totals.km, totals.litres, totals.kg_co2 (RPT-01). Re-dump openapi.json if schemas filled in.

    POST /routes/publish: body PublishIn.route_ids; empty list publishes all unpublished routes; sets Route.published true. GET /driver/route (PIN 0000) returns only published routes grouped by plate; optional query plate (D-19). Unpublished drafts must not appear (ASVS V4).

    GET /dispatcher: in main.py (or a dispatcher include) replace the 01-00 one-line HTML stub with fastapi.templating.Jinja2Templates TemplateResponse of templates/dispatcher.html (D-13). One page: button Seed, button Optimize, button Publish, div id=map height 480px, a totals strip showing km, litres, kg_co2. Leaflet 1.9.4 from unpkg CDN with the integrity hashes in 01-RESEARCH.md Code Examples. Tile URL https://tile.openstreetmap.org/{z}/{x}/{y}.png (HTTPS, DSP-02, D-05). Attribution text must include OpenStreetMap. setView [10.776, 106.700] zoom 12. After Optimize, JS fetch GET /routes and L.polyline each route.stops lat/lng using route.color. Do not add Leaflet via pnpm.

    Tests (write them in this task; tdd_mode is off so they may land with the code, but they must fail if the formula is wrong):
    - test_distance.py: HCMC_CIRCUITY == 1.35; road_km / haversine_km == 1.35
    - test_solver.py: maintenance vehicle unused; overload true when kg exceeds capacity; first and last stop kind depot
    - test_carbon.py: 100 km, diesel, l_per_100km 12 → litres 12, kg_co2 12*2.68
    - test_baseline.py: baseline uses excel_row order; on the zig-zag seed, optimized totals.km != baseline km (delta may be any non-zero; do not assert a marketing percent)
    - test_tracer_e2e.py: FastAPI TestClient with GREENLOGIX_DEMO=1; POST /seed, POST /optimize {"cluster_radius_km":3.0}, POST /routes/publish {"route_ids":[]}, GET /driver/route with X-Driver-Pin 0000 returns routes with nonempty stops, GET /dispatcher 200 and body contains tile.openstreetmap.org and OpenStreetMap

    Logging: order id not phone.
  </action>
  <verify>
    <automated>cd apps/api && GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed && uv run pytest -x tests/test_distance.py tests/test_solver.py tests/test_carbon.py tests/test_baseline.py tests/test_tracer_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/greenlogix_api/solver/distance.py contains HCMC_CIRCUITY = 1.35 (D-07, VRP-03)
    - emission_factors.json contains tank_to_wheel and 2.68 (D-09)
    - seed xlsx files exist under apps/api/data/seed/ and python -m greenlogix_api.seed exits 0 (ORD-02, D-18)
    - uv run pytest -x tests/test_distance.py tests/test_solver.py tests/test_carbon.py tests/test_baseline.py exits 0 (VRP-01..04, VEH-02, D-16, D-17)
    - GET /dispatcher HTML contains tile.openstreetmap.org and OpenStreetMap (DSP-02, D-05)
    - After seed, optimize, publish: GET /driver/route with X-Driver-Pin 0000 returns routes with nonempty stops (DRV-01, D-19)
    - dispatcher.html does not load Mapbox GL (D-04, D-05)
  </acceptance_criteria>
  <done>The single happy path seed → optimize → OSM map → publish → driver JSON works and is committed. Flutter list is Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flutter PIN 0000 shows published ordered stop list</name>
  <owner>thanh</owner>
  <hour_box>16h</hour_box>
  <precondition>GET /driver/route with X-Driver-Pin 0000 returns JSON routes with stops after seed+optimize+publish from Task 1.</precondition>
  <files>apps/mobile-driver/lib/screens/pin_screen.dart, apps/mobile-driver/lib/screens/route_list.dart, apps/mobile-driver/lib/api/client.dart, apps/mobile-driver/lib/models/driver_route.dart, apps/mobile-driver/test/route_list_test.dart, apps/mobile-driver/lib/main.dart</files>
  <read_first>apps/api/openapi.json, apps/mobile-driver/lib/api/client.dart, apps/mobile-driver/lib/models/driver_route.dart, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md</read_first>
  <behavior>
    - PinScreen with PIN 0000 calls ApiClient.getDriverRoute and navigates to RouteListScreen (D-19, DRV-01)
    - RouteListScreen lists stops in seq order showing address, phone, window_start, window_end, notes (DRV-01)
    - Empty routes array shows a Vietnamese empty state, not a fake hardcoded stop
    - 401 from the API stays on PinScreen with an error text
  </behavior>
  <action>
    WT-02 apps/mobile-driver/** only (D-01, D-12). If Task 1 re-dumped openapi.json with additive fields, update Dart models to match; do not invent a third contract (D-15).

    RouteListScreen: ListView of published routes grouped by plate (query ?plate= optional later). Each stop row shows seq, address, phone, window_start–window_end, notes. Sort by seq ascending. Do not implement Chỉ đường or status buttons here (those are 01-02).

    PinScreen: on 200 with routes, push RouteListScreen passing DriverRouteList. Document in a comment at the dart-define call site: Linux default http://127.0.0.1:8000; Android emulator http://10.0.2.2:8000.

    test/route_list_test.dart: pump RouteListScreen with a fixture of two stops out of seq order; expect the first visible address to be the seq=1 address. Second test: empty list finds the empty-state text.

    flutter analyze must be clean of errors. Do not add image_picker. Do not edit apps/api.
  </action>
  <verify>
    <automated>cd apps/mobile-driver && flutter test test/route_list_test.dart test/api_client_test.dart && flutter analyze --no-fatal-infos && grep -F 'RouteListScreen' lib/screens/route_list.dart && grep -F 'window_start' lib/screens/route_list.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/screens/route_list.dart contains RouteListScreen and displays address, phone, window_start, notes (DRV-01)
    - flutter test test/route_list_test.dart exits 0
    - PinScreen still sends X-Driver-Pin (D-19)
    - No maps_launcher in pubspec.yaml
  </acceptance_criteria>
  <done>A driver can type 0000 and see the published ordered stop list from FastAPI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Excel import row errors, vehicle registry, bilingual headers</name>
  <owner>chien</owner>
  <hour_box>6h</hour_box>
  <files>apps/api/src/greenlogix_api/ingest_xlsx.py, apps/api/src/greenlogix_api/routers/orders.py, apps/api/src/greenlogix_api/routers/vehicles.py, apps/api/tests/test_ingest.py, apps/api/templates/dispatcher.html</files>
  <read_first>apps/api/src/greenlogix_api/models.py, apps/api/templates/dispatcher.html, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md</read_first>
  <behavior>
    - POST /orders/import of a non-xlsx name returns 400 (ORD-01)
    - Missing lat or lng on a data row appends errors[] with field lat or lng and does not import that row (ORD-01, D-06)
    - Headers địa chỉ/address, vĩ độ/lat, kinh độ/lng, người nhận/receiver, SĐT/phone, khối lượng/kg, giờ bắt đầu/window_start, giờ kết thúc/window_end, loại hàng/cargo_type, ghi chú/notes all map (ORD-01)
    - Excel time cells normalize to HH:MM strings, never the serial 1899 date (pitfall 6)
    - GET /vehicles returns plate, type, capacity_kg, fuel, l_per_100km, status (VEH-01)
    - PATCH /vehicles/{id} {"status":"maintenance"} then POST /optimize leaves that plate unused (VEH-02)
    - Upload cap: reject files larger than 5_000_000 bytes and more than 500 data rows (ASVS V12)
  </behavior>
  <action>
    WT-01 (D-02). ingest_xlsx.py uses openpyxl load_workbook data_only=True read_only=True (ORD-01). Accept UploadFile via python-multipart on POST /orders/import. Return ImportResult. Do not geocode missing coordinates (D-06). Dispatcher.html: file input + submit to /orders/import and a table of GET /orders plus row-level error list from the last import. Vehicle table from GET /vehicles (VEH-01). PATCH status control ready|maintenance.

    Normalize window cells: if datetime or time, format HH:MM; if str, parse; never str(float serial) as the window.

    test_ingest.py covers the behavior bullets. Re-dump openapi.json if request bodies gained fields. Additive only (D-15).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -x tests/test_ingest.py tests/test_solver.py</automated>
  </verify>
  <acceptance_criteria>
    - ingest_xlsx.py maps both địa chỉ and address (ORD-01)
    - tests/test_ingest.py exits 0 including a missing-lat error case
    - GET /vehicles JSON includes capacity_kg and status (VEH-01)
    - dispatcher.html contains a file input for xlsx (DSP-01, ORD-01)
  </acceptance_criteria>
  <done>Dispatcher can seed or upload xlsx, see row errors, and manage vehicle ready/maintenance before optimize.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser dispatcher → FastAPI | Bearer DEMO, xlsx bytes, order PII |
| Flutter → FastAPI | PIN, stop list with phone/address |
| FastAPI → SQLite | Persisted PII in greenlogix.db |
| Dispatcher HTML → OSM tiles | Tile fetches must not include recipient phone or name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Information disclosure | seed xlsx phones | medium | mitigate | Fictional 09000000xx only; no real customer phones (Nghị định 13) |
| T-01-01-02 | Information disclosure | logger / exception traces | medium | mitigate | Log order id; do not interpolate phone |
| T-01-01-03 | Tampering | POST /orders/import | high | mitigate | xlsx extension, 5 MB cap, 500 row cap, openpyxl data_only, Pydantic row schema (ORD-01, ASVS V5/V12) |
| T-01-01-04 | Tampering | solver assignment | high | mitigate | overload true + unassigned_order_ids; never silently plan over capacity_kg (VRP-02) |
| T-01-01-05 | Information disclosure | Nominatim | high | mitigate | Do not send addresses to any geocoder; seed lat/lng (D-06, COST-01) |
| T-01-01-06 | Elevation | GET /driver/route | high | mitigate | Return published routes only; PIN required (D-19) |
| T-01-01-07 | Tampering | SQL | medium | mitigate | SQLModel parameters only |
| T-01-01-08 | Spoofing | OSM tiles | low | accept | Public tiles; HTTPS URL + attribution; no PII in tile URLs |
| T-01-SC | Tampering | no new PyPI packages | high | mitigate | No new installs this plan beyond 01-00 Approved set |
</threat_model>

<verification>
- [ ] GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed
- [ ] uv run pytest -x tests/test_distance.py tests/test_solver.py tests/test_carbon.py tests/test_baseline.py tests/test_ingest.py
- [ ] uv run pytest -x tests/test_tracer_e2e.py (seed + optimize + publish + /driver/route + dispatcher OSM URL)
- [ ] flutter test test/route_list_test.dart
- [ ] openapi.json still contains frozen paths; additive fields only
</verification>

<success_criteria>
- ORD-01, ORD-02, VEH-01, VEH-02, VRP-01..04, DSP-01 (map+optimize+publish), DSP-02, DRV-01, RPT-01 are observable
- D-04/D-05: OSM tiles, no Mapbox token
- D-07/D-09/D-16/D-17/D-18 implemented in code not comments
- Hour box: Chiến 20h, Thanh 16h
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-72h-walking-skeleton/01-01-SUMMARY.md`
</output>
