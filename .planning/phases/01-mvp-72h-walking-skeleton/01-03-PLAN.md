---
phase: 01-mvp-72h-walking-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - apps/api/src/greenlogix_api/routers/report.py
  - apps/api/src/greenlogix_api/routers/orders.py
  - apps/api/src/greenlogix_api/routers/driver.py
  - apps/api/templates/dispatcher.html
  - apps/api/tests/test_report.py
  - apps/api/tests/test_orders_crud.py
  - apps/api/openapi.json
  - apps/api/README.md
  - README.md
  - .gitignore
  - apps/api/data/emission_factors.json
  - apps/mobile-driver/pubspec.yaml
  - apps/mobile-driver/lib/screens/stop_detail.dart
  - apps/mobile-driver/lib/api/client.dart
  - apps/mobile-driver/README.md
  - apps/mobile-driver/test/photo_null_test.dart
autonomous: true
requirements:
  - ORD-03
  - DRV-04
  - RPT-02
  - COST-01
  - COST-02
user_setup: []

must_haves:
  truths:
    - "GET /report and the dispatcher report strip show baseline vs optimized km, litres, kg CO2 and percent deltas computed from the same HCMC_CIRCUITY and emission_factors.json — not marketing constants (RPT-02, D-09, D-17)."
    - "Dispatcher can PATCH and DELETE an order before optimize (ORD-03)."
    - "Optional POD photo uploads to local data/uploads with uuid names; status still works if the camera is denied (DRV-04, D-20)."
    - "Cold start of API plus Flutter against local SQLite needs only GREENLOGIX_DEMO=1 — no commercial geospatial credentials (COST-01, D-04, D-06)."
    - "Root README and apps/api/README.md contain the 72h jury click path: seed, optimize, map, Flutter list, Chỉ đường, delivered, report (COST-02)."
  artifacts:
    - path: "apps/api/src/greenlogix_api/routers/report.py"
      provides: "GET /report before/after (RPT-02)"
      contains: "km_pct"
    - path: "README.md"
      provides: "72h demo script (COST-02)"
      contains: "greenlogix_api.seed"
    - path: "apps/api/data/emission_factors.json"
      provides: "Cited TTW factors (D-09)"
      contains: "IPCC"
    - path: "apps/api/src/greenlogix_api/routers/orders.py"
      provides: "PATCH and DELETE /orders/{id} (ORD-03)"
    - path: "apps/mobile-driver/lib/screens/stop_detail.dart"
      provides: "Optional photo; status independent (DRV-04, D-20)"
  key_links:
    - from: "apps/api/src/greenlogix_api/routers/report.py"
      to: "apps/api/src/greenlogix_api/solver/baseline.py"
      via: "ReportOut.baseline vs optimized using stored totals"
      pattern: "baseline"
    - from: "apps/mobile-driver/lib/screens/stop_detail.dart"
      to: "apps/api/src/greenlogix_api/routers/driver.py"
      via: "POST /stops/{id}/photo multipart field photo"
      pattern: "/photo"
    - from: "README.md"
      to: "apps/api/src/greenlogix_api/seed.py"
      via: "documented uv run python -m greenlogix_api.seed"
      pattern: "greenlogix_api.seed"
---

## Phase Goal

**As a** HCMC dispatcher, **I want to** import today's Excel orders, get clustered routes that beat the old sequence on km and CO₂, and have tài xế execute the list in Flutter with a Maps deep-link, **so that** a contest jury sees dual value in one sitting.

## People split

| Task | owner | hour_box | Worktree | Branch |
|------|-------|----------|----------|--------|
| 1 | chien | 12h | WT-01 `../repo-cr001-api` | `cr/001-api` |
| 2 | chien | 12h | WT-01 `../repo-cr001-api` (root README, COST-02) | `cr/001-api` |
| 3 | thanh | 8h | WT-02 `../repo-cr001-flutter` | `cr/001-flutter` |

Wall clock hours 48–72 (D-03). Task 3 runs in parallel with Task 1 (frozen `POST /stops/{id}/photo` from 01-00; D-01 Thanh never edits root README). Task 2 follows Task 1 on WT-01. Chiến 24h sequential; Thanh 8h overlapping T1. Do not extend the calendar; cut Nominatim (already off), then Cloudflare Tunnel, then packages/shared-types TS — never cut RPT-02 computation.

<objective>
Close the contest loop: computed before/after km/litres/CO2 (RPT-02, D-17, D-09), order edit/delete (ORD-03), optional local POD photo (DRV-04, D-20), zero-key cold start (COST-01, D-04), and the jury README script (COST-02).

Purpose: Hours 48–72. Dual-value slide must be calculated from the 80/10 seed, not pasted from BRAINSTORM_IDEA.md.

Output: GET /report JSON+HTML, PATCH/DELETE orders UI, photo endpoint + image_picker best-effort, README demo script.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md
@apps/api/src/greenlogix_api/solver/baseline.py
@apps/api/data/emission_factors.json
@README.md
</context>

<artifacts_produced>
API: GET /report, PATCH/DELETE /orders/{id}, POST /stops/{id}/photo, tests/test_report.py, tests/test_orders_crud.py, dispatcher report strip + edit forms.

Docs: root README.md 72h section, apps/api/README.md commands, apps/mobile-driver/README.md Flutter jury steps.

Flutter: image_picker optional on delivered; skip on permission error.

CLI:
  GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed
  GREENLOGIX_DEMO=1 uv run uvicorn greenlogix_api.main:app --host 0.0.0.0 --port 8000
  curl -H 'Authorization: Bearer DEMO' http://127.0.0.1:8000/report
  flutter run --dart-define=API_BASE=http://10.0.2.2:8000

Env: GREENLOGIX_DEMO=1. Optional TZ=Asia/Ho_Chi_Minh. No other keys.

JSON ReportOut: baseline.{km,litres,kg_co2}, optimized.{km,litres,kg_co2}, delta.{km,litres,kg_co2,km_pct,litres_pct,kg_co2_pct}.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Computed before/after report, order PATCH/DELETE, photo endpoint</name>
  <owner>chien</owner>
  <hour_box>12h</hour_box>
  <precondition>Plan 01-01 stored baseline totals during POST /optimize; emission_factors.json exists; PATCH/DELETE /orders/{id} are in openapi.json.</precondition>
  <files>apps/api/src/greenlogix_api/routers/report.py, apps/api/src/greenlogix_api/routers/orders.py, apps/api/src/greenlogix_api/routers/driver.py, apps/api/templates/dispatcher.html, apps/api/tests/test_report.py, apps/api/tests/test_orders_crud.py, apps/api/data/emission_factors.json</files>
  <read_first>apps/api/src/greenlogix_api/solver/baseline.py, apps/api/src/greenlogix_api/carbon.py, apps/api/data/emission_factors.json, apps/api/templates/dispatcher.html, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md</read_first>
  <behavior>
    - GET /report returns ReportOut where delta.km equals optimized.km minus baseline.km (RPT-02, D-17)
    - km_pct equals delta.km / baseline.km * 100 when baseline.km != 0
    - The three percent fields are computed; the handler does not return the constants 8, 15, 5, 10, 12 as the delta (RPT-02)
    - Same HCMC_CIRCUITY and kg_co2_per_litre on both sides (D-07, D-09)
    - PATCH /orders/{id} with {"kg": 12} updates Order.kg; DELETE /orders/{id} removes the row; both require Bearer DEMO (ORD-03)
    - PATCH/DELETE after publish on an assigned order is 409 so the live route is not silently mutated
    - POST /stops/{id}/photo with multipart field photo stores under apps/api/data/uploads/{uuid}.jpg or .png; rejects non-image; 404 if unpublished (DRV-04, ASVS V12)
  </behavior>
  <action>
    WT-01 (D-02, D-09, D-17, ORD-03, RPT-02, DRV-04). GET /report reads the last optimize's stored baseline and optimized totals. Dispatcher.html: a report panel listing before, after, and percent for km, litres, kg_co2, labeled ước tính TTW CO₂ (IPCC/GLEC factors). Do not paste BRAINSTORM marketing ranges into the JSON.

    ORD-03: dispatcher forms for edit address/kg/window/notes and a delete button calling PATCH/DELETE /orders/{id} before Optimize. JSON keys match OrderPatch from 01-00.

    Photo: uuid4 filename, suffix whitelist jpg/jpeg/png, MIME image/jpeg or image/png, ignore client filename path, directory apps/api/data/uploads (gitignored). Status endpoints stay independent of photo (D-20).

    test_report.py: run baseline+optimize on a tiny in-memory set where the naive path is longer; assert delta.km is negative; assert the response body is not a canned marketing-percent fixture. test_orders_crud.py: PATCH then GET; DELETE then GET 404; 409 after publish.

    Re-dump openapi.json. Keep emission_factors.json citations (IPCC 2006 Table 1.4, GLEC v3.2).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -x tests/test_report.py tests/test_orders_crud.py tests/test_carbon.py tests/test_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - routers/report.py returns keys baseline, optimized, delta, km_pct (RPT-02)
    - tests/test_report.py exits 0 and computes percents from totals
    - orders.py implements PATCH and DELETE /orders/{id} (ORD-03)
    - driver.py stores photo under data/uploads with a uuid filename (DRV-04)
    - dispatcher.html contains the TTW CO2 label and edit/delete controls
  </acceptance_criteria>
  <done>Jury can read a computed before/after on /report and /dispatcher; dispatcher can edit/delete orders; photo is a local file write.</done>
</task>

<task type="auto">
  <name>Task 2: README 72h jury script and cost freeze</name>
  <owner>chien</owner>
  <hour_box>12h</hour_box>
  <files>README.md, apps/api/README.md, .gitignore</files>
  <read_first>README.md, apps/api/README.md, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md, apps/api/src/greenlogix_api/solver/distance.py</read_first>
  <action>
    WT-01 only for root README.md (D-01, D-02, COST-01, COST-02, D-03, D-04, D-07). Add a section to root README.md titled GreenLogix 72h contest demo without rewriting the landing CI docs.

    Required commands in that section, copy-pasteable:
    1. cd apps/api && uv python pin 3.12 && uv sync
    2. GREENLOGIX_DEMO=1 uv run python -m greenlogix_api.seed
    3. GREENLOGIX_DEMO=1 uv run uvicorn greenlogix_api.main:app --host 0.0.0.0 --port 8000
    4. Open http://127.0.0.1:8000/dispatcher — Seed (if needed), Optimize, see Leaflet OSM routes, Publish
    5. cd apps/mobile-driver && flutter run --dart-define=API_BASE=http://10.0.2.2:8000 (emulator) or the host LAN IP (phone) or http://127.0.0.1:8000 -d linux
    6. PIN 0000, open a stop, Chỉ đường, mark Đã giao, refresh dispatcher
    7. Open http://127.0.0.1:8000/report and read before/after km, litres, kg CO2

    Document named constant HCMC_CIRCUITY=1.35 (D-07) and that both baseline and optimized use it. Document depot 10.801, 106.661. Document GREENLOGIX_DEMO=1, Bearer DEMO, PIN 0000, localhost-only (D-19). Document emission_factors.json TTW petrol 2.31 diesel 2.68 with IPCC/GLEC citations (D-09). State that Nominatim is not on the required path because the seed xlsx has lat/lng (D-06, COST-01).

    Cost freeze: env samples may list GREENLOGIX_DEMO and TZ only. Do not add commercial geospatial credentials to any tracked env example. Confirm .gitignore includes apps/api/data/greenlogix.db and apps/api/data/uploads/.

    apps/api/README.md mirrors the uv commands. Write the Flutter jury steps in root README.md yourself (flutter devices, dart-define API_BASE for linux / 10.0.2.2 / LAN, PIN 0000, Chỉ đường, Đã giao, photo optional). D-01: Thanh does not edit root README.md; do not leave a subsection for Task 3 to fill. Document the seed workbook path apps/api/data/seed/hcmc_80_orders.xlsx.

    People split reminder: Thanh owns apps/mobile-driver; Chiến owns apps/api and root README demo section (D-01, D-02).
  </action>
  <verify>
    <automated>grep -F 'greenlogix_api.seed' README.md && grep -F 'HCMC_CIRCUITY' README.md && grep -F '0.0.0.0' README.md && grep -F '0000' README.md && grep -F 'dispatcher' README.md && grep -F '10.0.2.2' README.md && grep -F 'apps/api/data/seed/hcmc_80_orders.xlsx' README.md && grep -F 'greenlogix.db' .gitignore && grep -F 'uploads' .gitignore && python3 -c "
from pathlib import Path
import re
pat=re.compile(r'(MAPBOX|GOONG_API|GOOGLE_MAPS_API|HERE_API|OPENROUTESERVICE)', re.I)
hits=[]
for p in Path('.').rglob('*'):
    if any(x in p.parts for x in ('node_modules','.git','graphify-out','dist','build','.dart_tool')): continue
    if p.suffix.lower() not in {'.env','.md','.example','.yml','.yaml','.toml','.json','.py','.dart'} and p.name not in {'.env','.env.example'}:
        continue
    if p.name.startswith('.env') or p.name.endswith('.example') or p.name in {'README.md'} or 'README' in p.name:
        try: t=p.read_text(errors='ignore')
        except Exception: continue
        if pat.search(t) and p.name.startswith('.env'):
            hits.append(str(p))
assert not hits, hits
print('cost-freeze-ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - Root README.md contains greenlogix_api.seed, HCMC_CIRCUITY, dispatcher, PIN 0000, uvicorn 0.0.0.0, 10.0.2.2, and apps/api/data/seed/hcmc_80_orders.xlsx (COST-02, D-07, D-19)
    - .gitignore contains greenlogix.db and uploads (D-11)
    - Tracked .env examples contain no commercial geospatial key names (COST-01, D-04)
  </acceptance_criteria>
  <done>A jury can run the demo from README without asking either builder for hidden keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Optional image_picker POD (null catch)</name>
  <owner>thanh</owner>
  <hour_box>8h</hour_box>
  <precondition>Frozen OpenAPI path POST /stops/{id}/photo exists in apps/api/openapi.json from plan 01-00 (empty/503 body allowed). WT-02 ../repo-cr001-flutter has StopDetailScreen from 01-02. Does not wait on Task 1 storage or Task 2 root README.</precondition>
  <files>apps/mobile-driver/pubspec.yaml, apps/mobile-driver/lib/screens/stop_detail.dart, apps/mobile-driver/lib/api/client.dart, apps/mobile-driver/README.md, apps/mobile-driver/test/photo_null_test.dart</files>
  <read_first>apps/mobile-driver/lib/screens/stop_detail.dart, apps/mobile-driver/lib/api/client.dart, apps/api/openapi.json, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md</read_first>
  <behavior>
    - ImagePicker null or thrown exception still calls postStatus delivered with no photo field (DRV-04, D-20)
    - Chosen file POSTs multipart field photo to /stops/{id}/photo with X-Driver-Pin then postStatus delivered
  </behavior>
  <action>
    WT-02 only (D-01, DRV-04, D-20). Parallel with Task 1; the photo path is frozen in 01-00. flutter pub add image_picker (Approved first-party). On Đã giao, try ImagePicker.pickImage. If permission fails or the user cancels, still call postStatus delivered with no photo and show a short snackbar that status saved. If a file is chosen, POST multipart field name photo to /stops/{id}/photo with X-Driver-Pin then postStatus delivered.

    Catch exceptions from the picker — never block the jury on the camera (D-20).

    test/photo_null_test.dart: when the picker returns null, postStatus delivered is invoked and no multipart photo call is made.

    Write flutter devices, dart-define API_BASE values for linux / 10.0.2.2 / LAN, PIN 0000, Chỉ đường, Đã giao, photo optional into apps/mobile-driver/README.md only. Do not edit root README.md (COST-02 greps are Chiến Task 2). Do not edit apps/api Python.

    If an Android device is available, run flutter test and a smoke flutter run; if only Linux, the photo-null test is the gate.
  </action>
  <verify>
    <automated>cd apps/mobile-driver && grep -F 'image_picker' pubspec.yaml && grep -F 'photo' lib/api/client.dart && grep -F 'ImagePicker' lib/screens/stop_detail.dart && flutter test test/photo_null_test.dart && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains image_picker (DRV-04)
    - test/photo_null_test.dart exits 0 covering picker-null still posting delivered (D-20)
    - apps/mobile-driver/README.md contains 10.0.2.2 and PIN 0000
    - flutter test exits 0
  </acceptance_criteria>
  <done>Photo is best-effort; the contest loop works without a camera. Root README.md is untouched by Thanh (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter camera → local file → FastAPI | POD image bytes; PII of the recipient may appear in the photo |
| Dispatcher → FastAPI | Order edits; report JSON |
| FastAPI → data/uploads | Files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Tampering | POST /stops/{id}/photo | high | mitigate | uuid4 name, jpg/png suffix whitelist, MIME check, never use client path, unpublished 404 (ASVS V12, DRV-04) |
| T-01-03-02 | Information disclosure | POD photos | medium | mitigate | Store only under apps/api/data/uploads (gitignored); do not proxy to OSM or a CDN |
| T-01-03-03 | Tampering | GET /report | high | mitigate | Compute deltas from stored totals; tests reject hardcoded marketing percents (RPT-02) |
| T-01-03-04 | Elevation | PATCH/DELETE orders | medium | mitigate | Bearer DEMO + GREENLOGIX_DEMO; 409 after publish (ORD-03, D-19) |
| T-01-03-05 | Information disclosure | README demo PIN | high | accept | D-19 localhost contest; README says localhost-only |
| T-01-03-06 | Information disclosure | logs | medium | mitigate | No phone in log lines |
| T-01-SC | Tampering | image_picker | high | mitigate | First-party Flutter package from RESEARCH.md Approved list |
</threat_model>

<verification>
- [ ] uv run pytest -x tests/test_report.py tests/test_orders_crud.py
- [ ] curl GET /report after seed+optimize returns baseline and optimized objects
- [ ] flutter test
- [ ] README contains seed, dispatcher, 0000, HCMC_CIRCUITY, report, apps/api/data/seed/hcmc_80_orders.xlsx
- [ ] gitignored db + uploads
</verification>

<success_criteria>
- RPT-02, ORD-03, DRV-04, COST-01, COST-02 true
- D-03 calendar not extended
- D-04/D-06: seed lat/lng path; Nominatim unwired
- Hour box: Chiến 24h, Thanh 8h
- Combined hour boxes across 01-00..01-03: Chiến 4+20+10+24=58h, Thanh 3+16+24+8=51h, both ≤72h wall with overlap
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-72h-walking-skeleton/01-03-SUMMARY.md`
</output>
