---
phase: 01-mvp-72h-walking-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/api/src/greenlogix_api/routers/driver.py
  - apps/api/src/greenlogix_api/models.py
  - apps/api/templates/dispatcher.html
  - apps/api/tests/test_status.py
  - apps/api/openapi.json
  - apps/mobile-driver/lib/screens/route_list.dart
  - apps/mobile-driver/lib/screens/stop_detail.dart
  - apps/mobile-driver/lib/api/client.dart
  - apps/mobile-driver/lib/api/maps_link.dart
  - apps/mobile-driver/test/maps_link_test.dart
  - apps/mobile-driver/test/status_test.dart
  - apps/mobile-driver/android/app/src/debug/AndroidManifest.xml
  - apps/mobile-driver/android/app/src/debug/res/xml/network_security_config.xml
  - apps/mobile-driver/ios/Runner/Info.plist
  - apps/mobile-driver/README.md
autonomous: true
requirements:
  - DRV-02
  - DRV-03
  - DSP-01
user_setup:
  - service: android-avd-or-phone
    why: "DRV-02 Chỉ đường opens an OS Maps app; Linux desktop cannot prove the intent. Unit tests still prove URL construction."
    dashboard_config:
      - task: "Start an Android emulator or plug a phone by hour 24 of the 72h clock"
        location: "Android Studio AVD Manager, or USB debugging"
        details: "flutter devices must list android. API_BASE=http://10.0.2.2:8000 on emulator; LAN IP on a physical phone. uvicorn --host 0.0.0.0 --port 8000"

must_haves:
  truths:
    - "Each stop has Chỉ đường that opens Google Maps, Apple Maps, or a geo: URI with the stop lat,lng (DRV-02, D-08)."
    - "Driver marks arrived, delivered, or failed with reason enum; POST /stops/{id}/status persists; dispatcher HTML shows the new status after refresh (DRV-03, DSP-01)."
    - "Publish remains the only way a draft route appears in GET /driver/route (DSP-01, D-19)."
  artifacts:
    - path: "apps/mobile-driver/lib/api/maps_link.dart"
      provides: "Google then Apple then geo: URIs (D-08)"
      contains: "google.com/maps/dir"
    - path: "apps/mobile-driver/lib/screens/stop_detail.dart"
      provides: "Chỉ đường + status buttons (DRV-02, DRV-03)"
      contains: "arrived"
    - path: "apps/api/src/greenlogix_api/routers/driver.py"
      provides: "POST /stops/{id}/status"
      contains: "fail_reason"
    - path: "apps/mobile-driver/android/app/src/debug/res/xml/network_security_config.xml"
      provides: "Debug cleartext HTTP to the LAN API"
      contains: "cleartextTrafficPermitted"
  key_links:
    - from: "apps/mobile-driver/lib/api/maps_link.dart"
      to: "https://www.google.com/maps/dir/"
      via: "url_launcher LaunchMode.externalApplication, no Maps SDK key (D-04, D-08)"
      pattern: "maps/dir"
    - from: "apps/mobile-driver/lib/screens/stop_detail.dart"
      to: "apps/api/src/greenlogix_api/routers/driver.py"
      via: "POST /stops/{id}/status JSON StatusIn"
      pattern: "/stops/"
    - from: "apps/api/templates/dispatcher.html"
      to: "apps/api/src/greenlogix_api/models.py"
      via: "GET /routes includes Stop.status after writeback"
      pattern: "status"
---

## Phase Goal

**As a** HCMC dispatcher, **I want to** import today's Excel orders, get clustered routes that beat the old sequence on km and CO₂, and have tài xế execute the list in Flutter with a Maps deep-link, **so that** a contest jury sees dual value in one sitting.

## People split

| Task | owner | hour_box | Worktree | Branch |
|------|-------|----------|----------|--------|
| 1 | chien | 10h | WT-01 `../repo-cr001-api` | `cr/001-api` |
| 2 | thanh | 14h | WT-02 `../repo-cr001-flutter` | `cr/001-flutter` |
| 3 | thanh | 10h | WT-02 `../repo-cr001-flutter` | `cr/001-flutter` |

Wall clock hours 24–48 (D-03). Task 2 and Task 3 are Thanh sequential. Task 1 (Chiến) overlaps them.

<objective>
Driver execution slice: OS Maps deep-link (DRV-02, D-08) and arrived/delivered/failed writeback visible on dispatcher refresh (DRV-03, DSP-01 publish already in 01-01). Photo upload stays 01-03 (D-20: status is mandatory, photo is not).

Purpose: Hours 24–48 of the 72h clock.

Output: maps_link.dart, stop_detail.dart, POST /stops/{id}/status, debug cleartext + Android queries, dispatcher status column.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md
@apps/api/openapi.json
@apps/mobile-driver/lib/screens/route_list.dart
</context>

<artifacts_produced>
API: POST /stops/{id}/status implemented; Stop.status and fail_reason persisted; dispatcher refresh lists status; tests/test_status.py.

Flutter: maps_link.dart, stop_detail.dart, Android debug network_security_config, AndroidManifest queries, iOS LSApplicationQueriesSchemes.

CLI: curl -H 'X-Driver-Pin: 0000' -H 'Content-Type: application/json' -d '{"status":"delivered"}' POST http://127.0.0.1:8000/stops/1/status

Env: GREENLOGIX_DEMO=1. dart-define API_BASE=http://10.0.2.2:8000

JSON: StatusIn.status arrived|delivered|failed; StatusIn.reason khach_vang|sai_dia_chi|hang_hong|tu_choi when failed.

Dart: openChiDuong(lat,lng,label), StopDetailScreen, ApiClient.postStatus.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist stop status and show it on dispatcher refresh</name>
  <owner>chien</owner>
  <hour_box>10h</hour_box>
  <precondition>Plan 01-01 tracer: published routes exist in SQLite after seed+optimize+publish; POST /stops/{id}/status is registered in openapi.json.</precondition>
  <files>apps/api/src/greenlogix_api/routers/driver.py, apps/api/src/greenlogix_api/models.py, apps/api/templates/dispatcher.html, apps/api/tests/test_status.py, apps/api/openapi.json</files>
  <read_first>apps/api/src/greenlogix_api/routers/driver.py, apps/api/src/greenlogix_api/models.py, apps/api/templates/dispatcher.html, apps/api/openapi.json, .planning/phases/01-mvp-72h-walking-skeleton/01-CONTEXT.md</read_first>
  <behavior>
    - POST /stops/{id}/status with X-Driver-Pin 0000 and body {"status":"arrived"} sets Stop.status to arrived and returns StatusOut (DRV-03, D-19, D-20)
    - {"status":"failed"} without reason returns 422; with reason khach_vang stores fail_reason (DRV-03)
    - GET /routes after writeback includes that stop's status so dispatcher HTML can paint it (DSP-01)
    - POST /stops/{id}/status on an unpublished route returns 404 (PIN must not mutate drafts)
    - Dispatcher page shows per-stop status text and a refresh control that re-fetches GET /routes
  </behavior>
  <action>
    WT-01 only (D-02). Implement POST /stops/{id}/status in routers/driver.py using require_driver. Allowed status values: arrived, delivered, failed. reason enum: khach_vang, sai_dia_chi, hang_hong, tu_choi. Persist Stop.status and Stop.fail_reason. Also copy delivered/failed onto Order.status when kind is stop.

    Join Stop to Route and require Route.published is true, else 404.

    dispatcher.html: render each stop's status next to the address; a Refresh button calls GET /routes and updates the list without a full navigation (plain fetch is enough; no HTMX required). Publish button from 01-01 stays.

    tests/test_status.py uses TestClient with GREENLOGIX_DEMO=1: seed-or-insert one published stop, POST arrived, GET /routes contains arrived; failed missing reason → 422; unpublished stop → 404.

    Re-dump openapi.json additive only (D-15). Do not log phone. Do not implement photo storage here (D-20, 01-03).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest -x tests/test_status.py</automated>
  </verify>
  <acceptance_criteria>
    - routers/driver.py handles POST status arrived, delivered, failed (DRV-03)
    - tests/test_status.py exits 0 including 422 on failed without reason
    - dispatcher.html displays stop status after GET /routes (DSP-01)
    - Unpublished route stops cannot be updated (401/404)
  </acceptance_criteria>
  <done>Marking delivered on the API is visible on dispatcher refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chỉ đường via url_launcher (Google, then Apple, then geo:)</name>
  <owner>thanh</owner>
  <hour_box>14h</hour_box>
  <files>apps/mobile-driver/lib/api/maps_link.dart, apps/mobile-driver/lib/screens/stop_detail.dart, apps/mobile-driver/lib/screens/route_list.dart, apps/mobile-driver/test/maps_link_test.dart, apps/mobile-driver/android/app/src/debug/AndroidManifest.xml, apps/mobile-driver/android/app/src/debug/res/xml/network_security_config.xml, apps/mobile-driver/ios/Runner/Info.plist, apps/mobile-driver/README.md</files>
  <read_first>apps/mobile-driver/lib/screens/route_list.dart, apps/mobile-driver/pubspec.yaml, .planning/phases/01-mvp-72h-walking-skeleton/01-RESEARCH.md</read_first>
  <behavior>
    - openChiDuong(lat,lng,label) builds the Google Maps dir URL with query api=1 and destination=lat,lng, then the Apple Maps daddr URL, then geo:lat,lng?q=lat,lng(label) (DRV-02, D-08)
    - launchUrl uses LaunchMode.externalApplication; on failure try the next URI
    - StopDetailScreen has a button labeled Chỉ đường that calls openChiDuong with that stop's lat/lng/address
    - maps_link_test.dart asserts the three URI strings for (10.776, 106.700, "Q1")
  </behavior>
  <action>
    WT-02 only (D-01, D-08, D-12). Use url_launcher already in pubspec. Do not add maps_launcher.

    maps_link.dart: function openChiDuong as specified in 01-RESEARCH.md url_launcher Maps deep-link section. Google Maps URLs do not use an API key (D-04).

    Android debug: network_security_config.xml with cleartextTrafficPermitted true for debug builds only so http://10.0.2.2:8000 works. AndroidManifest.xml application android:networkSecurityConfig plus queries for geo, https, http, google.navigation. iOS Info.plist LSApplicationQueriesSchemes: comgooglemaps, maps; NSAppTransportSecurity NSAllowsArbitraryLoads only in Debug if the Runner debug entitlements file exists — do not enable cleartext in release.

    README: flutter run --dart-define=API_BASE=http://10.0.2.2:8000 for emulator; LAN IP for a physical phone; uvicorn --host 0.0.0.0. Note that Linux desktop can list stops but cannot prove OS Maps (user_setup).

    If flutter devices lists no android target, still land the URLs and tests; do not block the plan. Start an AVD when the SDK has one: emulator -list-avds then flutter emulators --launch, or document the gap in the SUMMARY.

    RouteListScreen: tapping a stop opens StopDetailScreen. Chỉ đường button on the detail screen.
  </action>
  <verify>
    <automated>cd apps/mobile-driver && flutter test test/maps_link_test.dart && grep -F 'google.com/maps/dir' lib/api/maps_link.dart && grep -F 'maps.apple.com' lib/api/maps_link.dart && grep -F 'geo:' lib/api/maps_link.dart && grep -F 'Chỉ đường' lib/screens/stop_detail.dart && grep -F 'cleartextTrafficPermitted' android/app/src/debug/res/xml/network_security_config.xml && grep -F 'queries' android/app/src/debug/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - maps_link.dart contains google.com/maps/dir, maps.apple.com, and geo: (DRV-02, D-08)
    - stop_detail.dart contains the label Chỉ đường
    - flutter test test/maps_link_test.dart exits 0
    - debug network_security_config.xml contains cleartextTrafficPermitted
    - pubspec.yaml still has no maps_launcher dependency
  </acceptance_criteria>
  <done>Chỉ đường constructs and launches OS Maps URLs without any billed Maps SDK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Status buttons write arrived / delivered / failed to the API</name>
  <owner>thanh</owner>
  <hour_box>10h</hour_box>
  <precondition>POST /stops/{id}/status from Task 1 returns 200 for a published stop.</precondition>
  <files>apps/mobile-driver/lib/screens/stop_detail.dart, apps/mobile-driver/lib/api/client.dart, apps/mobile-driver/lib/models/status_update.dart, apps/mobile-driver/test/status_test.dart</files>
  <read_first>apps/mobile-driver/lib/api/client.dart, apps/api/openapi.json, apps/mobile-driver/lib/screens/stop_detail.dart</read_first>
  <behavior>
    - StopDetailScreen buttons Đã đến, Đã giao, Thất bại call POST /stops/{id}/status with StatusIn (DRV-03, D-20)
    - Thất bại requires picking one of khach_vang, sai_dia_chi, hang_hong, tu_choi before POST
    - 200 updates local Stop.status so the list reflects the new value
    - Camera is not required; status succeeds with no photo field (D-20)
  </behavior>
  <action>
    WT-02 (D-01). ApiClient.postStatus(id, StatusIn) sends JSON keys status and reason, header X-Driver-Pin (D-19). Do not attach multipart photo in this plan.

    Status buttons on StopDetailScreen. On failed, show a simple picker of the four reason enum values. test/status_test.dart: StatusIn toJson for delivered has reason null; for failed includes reason khach_vang.

    Never log phone. Do not add image_picker yet.
  </action>
  <verify>
    <automated>cd apps/mobile-driver && flutter test test/status_test.dart test/maps_link_test.dart && grep -F 'postStatus' lib/api/client.dart && grep -E 'arrived|delivered|failed' lib/screens/stop_detail.dart && grep -F 'khach_vang' lib/screens/stop_detail.dart</automated>
  </verify>
  <acceptance_criteria>
    - client.dart contains postStatus posting to /stops/ (DRV-03)
    - stop_detail.dart contains arrived, delivered, failed, and khach_vang
    - flutter test test/status_test.dart exits 0
    - No image_picker in pubspec.yaml yet (D-20 photo is 01-03)
  </acceptance_criteria>
  <done>Driver status writeback works without a camera.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter → OS Maps app | lat/lng and label only; must not put phone in the Maps query string |
| Flutter → FastAPI | PIN + status JSON; phone already on the stop record |
| Dispatcher browser → FastAPI | Reads updated status |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Information disclosure | maps_link.dart | medium | mitigate | URI query is lat,lng and address label only; do not append phone |
| T-01-02-02 | Elevation | POST /stops/{id}/status | high | mitigate | require_driver + published-route check; unpublished 404 |
| T-01-02-03 | Tampering | StatusIn | medium | mitigate | Enum validation; failed requires reason; 422 otherwise |
| T-01-02-04 | Information disclosure | cleartext HTTP | medium | mitigate | Debug-only network_security_config; contest LAN; not in release |
| T-01-02-05 | Information disclosure | logs | medium | mitigate | Do not log phone on status POST |
| T-01-SC | Tampering | pub packages | high | mitigate | url_launcher already Approved; do not add maps_launcher |
</threat_model>

<verification>
- [ ] uv run pytest -x tests/test_status.py
- [ ] flutter test test/maps_link_test.dart test/status_test.dart
- [ ] curl POST delivered on a published stop then GET /routes shows delivered
- [ ] maps_link.dart has the three URI families
</verification>

<success_criteria>
- DRV-02, DRV-03, DSP-01 publish+status loop are true
- D-04/D-08: no Maps SDK key; url_launcher only
- D-20: status works with no photo
- Hour box: Chiến 10h, Thanh 24h
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-72h-walking-skeleton/01-02-SUMMARY.md`
</output>
